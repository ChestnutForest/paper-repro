# 開発ログ: paper-repro — 2026-09-03（JST）

## メタ情報
- 日付（JST）: 2026-09-03
- 本日の作業範囲: 要件定義の構造決定（主題2「利用者の状態」の選択と反映、既存サブ要求の扱いを決める第7バッチ）と、USDM（Universal Specification Describing Manner、要求を振る舞いの動詞形で書き仕様へ分解する記法）仕様展開の着手（`REQ-C03` の展開）。コミット8件。実装コードには触れていない。この日の作業は claude.ai のチャットで行い、Claude Code / Codex の履歴には残っていない。素材ファイル `devlog-source-2026-09-03.md` の内容だけを根拠にこのログを生成した。
- 参照したソース URL:
  - 本日は該当なし
- ⚠️ 日付の境界に関する注記: コミット `ced10af`（`REQ-C05` の展開）は2026-09-04 05:29 の実施であり、本日分には含めない。素材ファイル自身がこの境界を明記しており、翌日のログで扱う対象とした。

## Q&A 知識カード
その日に理解したことを、問いと答えの形で残す。各カードは単独で読めるように書く。

### Q. `REQ-C02-S01`（既読資料の横断保持）を採用した結果、データモデルはどうなったか？
A. 18番目のエンティティ `DM-E18 LearningProfile` を新設した。既存17エンティティは `DM-E01`〜`DM-E17` のすべてが `Project`（プロジェクト）の配下にあり、プロジェクトを横断して利用者に張り付く属性を持つ場所が無かった。学習記録は `SelfExplanation`・`DeepDiveQueue`・`Question` の3件あるが、いずれもプロジェクトに紐づく記録である。
（出典: 本日の作業から）

### Q. `DM-E18` が他の17エンティティと違う点は何か？
A. `Project` に紐づかない唯一のエンティティである。利用者に紐づき、プロジェクトを横断して保持する。2本目以降の論文で既習の項目を省くために必要だからである。主管は「利用者（自己申告のみ）」とし、熟達段階や理解度は保持しない。
（出典: 本日の作業から）

### Q. `DM-E18` を追加したとき、なぜ他の成果物へ反映しなかったのか？
A. 件数の書き換えではなく、設計判断を含むためである。ER図（エンティティと関連を図示した設計文書）は4枚ともすべて `PROJECT` を起点としており、`DM-E18` はどこにも収まらない。「既読論文」が内部の `Paper` レコードなのか外部の申告なのかも未定である。47業務に「既読資料を登録する」業務も無い。定義が17件しかない状態で件数だけ18へ書き換えると、不整合を移動させるだけになる。決めるべき事項を `worknotes/pending-dm-e18-propagation.md` へ記録し、次の作業単位へ送った。
（出典: 本日の作業から）

### Q. USDM 展開の前に決めるべきだったことは何か？
A. `requirements-usdm.md` 第4章が4件の未解決事項を挙げていたが、そのうち1件だけが展開作業を止めていた。「正本をどちらにするか」は展開完了後でよい（同書が「展開が済んだ時点で判断する」と定めているため）。「サブ要求の扱い」は今決める必要があり、第7バッチとして5択（要求候補を採用・却下などの区分に振り分ける手順）を作成した。「展開の順序」は構造の決定後でよく、「非機能要求の書き方」は展開の後半でよいと判断した。
（出典: 本日の作業から）

### Q. なぜサブ要求の扱いだけが展開を止めたのか？
A. `REQ-C01` の展開では問題が起きなかった。`REQ-C01` に既存サブ要求が無かったからである。しかし `REQ-C05` には `S01`〜`S03` があり、展開すると同じ要求の下に `Sxx`（既存サブ要求）と `Rn`（USDM 下位要求）が並ぶことになる。両者は別の概念である。`Sxx` は一次資料の分析と5択を経て確定した要求、`Rn` はメイン要求を振る舞いへ分解した中間層で、仕様を持つ。
（出典: 本日の作業から）

### Q. 第7バッチ（文書構造の5択）で選択肢3を選んだ決め手は何か？
A. `REQ-C02-S01` である。既存サブ要求15件は性格が一様でなく、`REQ-C02-S01`（既読資料の横断保持）はメイン要求 `REQ-C02` に無い機能を足している。`REQ-C02` は「前提知識を段階的に説明し、一次資料へ戻せる」ことを求めるが、既読資料の保持はそこから導けない。選択肢4（メイン要求から直接展開し、サブ要求は入力として参照）を採ると、この要求が USDM の階層から消え、要求仕様書を読んだだけでは機能の存在が分からなくなる。
（出典: 本日の作業から）

### Q. `Sxx` と `Rn` の記法が混在することは欠陥か？
A. 欠陥ではなく情報である。由来が違うものに違う記号を使うことで、その要求がどこから来たかが読み取れる。`Sxx` は5択を経て確定した要求、`Rn` は振る舞いの分解である。
（出典: 本日の作業から）

### Q. USDM 展開の順序をどう決めたか？
A. 依存関係から導いた。1番目は `REQ-C03`（論文の取り込み。論文が入らなければ他の工程が始まらないため）。2番目は `REQ-C02`・`REQ-C04`（読解・学習）と `REQ-C05`（再現実装）（`REQ-C01` が両者を同等の主要経路と定めるため片方だけ先行させない）。3番目は `REQ-C06`（実行言語と承認ゲート）。4番目は `REQ-C07`・`REQ-C08`（証拠と由来。何を記録するかが決まってから）。5番目は `REQ-C09`〜`REQ-C11`（第8章が段階2以降を第2フェーズへ先送りしているため最後）。
（出典: 本日の作業から）

### Q. `REQ-C03` の展開で、外部インタフェース仕様との重複をどう避けたか？
A. 取り込みの手順そのものは `IF-01`（外部インタフェース仕様書）が定めるため、USDM の章は重複して書かない。リクエストの組み立てや応答の項目は `arch-guide/interfaces/IF-01.md` にあり、USDM 側は判別と後続の振る舞いだけを定めた。たとえば `REQ-C03-R2.50`「HTTP の状態コードだけで判別しない」は、arXiv がエラーを HTTP 200 の正常な形式で返すという `IF-01` 1.4節の事実を前提としている。
（出典: 本日の作業から）

### Q. `REQ-C03-S01.60` を独立した仕様にしたのはなぜか？
A. 「不明を `unknown` として扱う」だけでは、取得できなかったのか、取得した結果が空だったのかを区別しない実装が成立してしまう。前者は再取得で埋まる可能性があり、後者は埋まらない。後の判断が変わるため、区別を仕様として明示した。
（出典: 本日の作業から）

### Q. USDM 展開の1要求あたりの分量はどれくらいか？
A. 実測値は2件。`REQ-C01` は下位要求3件・仕様28件・約155行。`REQ-C03` は下位要求2件＋既存サブ要求1件・仕様31件・約181行。要求あたり160〜180行、仕様30件前後という傾向が見えた。残り9要求で約1,500行・仕様270件の見込みとなり、当初の見積り（1,550行・280件）とほぼ一致した。
（出典: 本日の作業から）

## 開発の型の出現記録
このプロジェクトの開発ならではの「型」を記録する。前日までの devlog はこの会話中に無いため、累積頻度は更新しない。本日分のみを記載する。累積更新には前日の devlog を貼ってください。

### 決定ログ（なぜその技術・設計を選んだか）
| 決定した事項 | 選んだ理由 | 却下した代替案 | 本日 |
|---|---|---|---|
| `DM-E18 LearningProfile` を新設 | `REQ-C02-S01`（既読資料の横断保持）の保持先が既存17エンティティに無いため | 既存エンティティのいずれかへ無理に持たせる | 本日1 |
| `DM-E18` の他成果物への反映を次の作業単位へ送る | 件数の書き換えではなく設計判断（ER図の描き方、既読論文の内外区別、業務の要否）を含むため | 件数だけをその場で18件へ書き換える | 本日1 |
| 第7バッチ（文書構造の5択）で選択肢3を採用 | `REQ-C02-S01` のようにメイン要求に無い機能を足すサブ要求があり、既存サブ要求はIDを保ったまま下位要求と同じ階層へ置くべきと判断したため | 選択肢4（メイン要求から直接展開しサブ要求は入力として参照） | 本日1 |
| USDM展開の順序を依存関係で決定 | 論文の取り込みが最初、第8章が段階2以降を先送りしている要求群が最後という依存の上流から順に展開するため | 分析文書が作られた順や要求番号順で展開する | 本日1 |
| 1回の作業単位を下位要求1件とする | `REQ-C01-R1` の実測（仕様11件・約35行）にもとづき、単位の大きさを揃えるため | 要求（メイン要求）単位で一括展開する | 本日1 |

### つまずき→解決の記録
| つまずき | 原因 | 解決方法 | 本日 |
|---|---|---|---|
| `requirements-usdm.md` へ `REQ-C03` の展開を挿入する際、改行コードを壊してファイルを差し戻す事態になった（225行の文書が865行になり、差分が452行追加・285行削除という異常な規模になった） | `[IO.File]::WriteAllLines($p, $t.Split([char]10), (New-Object Text.UTF8Encoding $false))` を使ったところ、ファイルがCRLF（Windowsの改行コード、キャリッジリターン+ラインフィード）を含んでいたため、LFで分割した各行の末尾に `\r` が残り、`WriteAllLines` がそこへさらに改行を付け、全行の間に空行が挿入された | `git checkout -- docs/requirements-usdm.md` で差し戻し、Claude Code に統合を依頼して解決した。教訓として、文字列操作（`ReadAllText`／`WriteAllText`）と行操作（`ReadAllLines`／`WriteAllLines`）を混ぜないこと、`Split` した結果を `WriteAllLines` へ渡さないこと、章の挿入や番号の付け替えのような複数箇所にまたがる編集はPowerShellで組み立てるよりClaude Codeへ依頼するほうが安全であることを得た。想定は180行前後の追加だったが実際は452行追加・285行削除であり、`git diff --stat` を編集直後に見る習慣が被害を1コミット分に留めた | 本日1 |

### Git・環境操作の記録
| 操作 | コマンド/手順 | 効果 | 本日 |
|---|---|---|---|
| 日をまたいだ作業のコミット数を確認する | `git --no-pager log --since="2026-09-03 00:00" --format="%h %cd %s" --date=format:"%m-%d %H:%M"` | セッションの開始からの累計を「今日のコミット数」として報告していた誤りに気づき、正しい日付範囲のコミット数を確認できた。日付は `git log` が持っているため、会話の流れではなくそちらを見るという教訓を得た。実際は2026-09-01から2026-09-03までの3日間にまたがっていた | 本日1 |

## 語彙・用語リスト（プロジェクト固有）
| 用語 | 定義 |
|---|---|
| `DM-E18 LearningProfile` | 18番目のエンティティ。既読論文・既学習資料・専門分野を保持する。`Project` に紐づかない唯一のエンティティである |
| `Sxx`（既存サブ要求） | 一次資料の分析と5択を経て確定した要求。`REQ-C05-S01` などがこれにあたる |
| `Rn`（USDM 下位要求） | メイン要求を振る舞いへ分解した中間層。仕様を持つ。`REQ-C01-R1` などがこれにあたる |
| 仕様の付番規則 | `REQ-Cxx-Sxx.10` および `REQ-Cxx-Rn.10` の形式。10刻みで付番し、後から挿入できるようにする |

## 未解決の疑問と翌日の開発計画
- 本日解決した項目:
  - 主題2（利用者の状態）の選択。選択肢3を採用し、要件v0.3.1へ反映済み
  - 第7バッチ（文書構造）。選択肢3を採用し、既存サブ要求はIDを保つこととした
  - USDM展開の順序を決定した
  - `REQ-C03` の展開。仕様31件
- 未解決の疑問:
  - `DM-E18` の追随。ER図での描き方、既読論文の内部・外部の区別、業務追加の要否が未定で、`worknotes/pending-dm-e18-propagation.md` に記録済み
  - 主題3（`ARF-M-08` 貢献の記録）の5択が未作成
  - `PR-M-13` 本体（設計原理の抽象化）の扱いが未定
  - USDM展開が11要求中2件にとどまっている。残り9要求、約1,500行・仕様270件の見込み
  - `REQ-C08-S01` の具体化、`REQ-C09-S04` の保留解除が未着手
  - 1.2.3系の分析4件が要件へ未反映。`REQ-C04-S03`・`REQ-C10-S05` が候補のまま
- 翌日の計画:
  - `REQ-C05`（再現実装）の展開。展開順序の2番目にあたる
  - `REQ-C02`・`REQ-C04`（読解・学習）の展開
  - 主題3の5択
  - `DM-E18` の追随

## 画面キャプチャ記録
本日は画面キャプチャの共有なし
