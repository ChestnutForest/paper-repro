# 開発ログ: paper-repro — 2026-09-02（JST）

## メタ情報
- 日付（JST）: 2026-09-02
- 本日の作業範囲: 要求分析の突合（前日までに溜まっていた4つの分析文書の要求候補36件を、現行の確定要求23件と突き合わせ、4領域の突合表を作成）。コミット5件。実装コードには触れていない。この日の作業は claude.ai のチャットで行い、Claude Code / Codex の履歴には残っていない。素材ファイル `devlog-source-2026-09-02.md` の内容だけを根拠にこのログを生成した。
- 参照したソース URL:
  - 本日は該当なし

## Q&A 知識カード
その日に理解したことを、問いと答えの形で残す。各カードは単独で読めるように書く。

### Q. 突合表（crosswalk、複数の分析文書の要求候補を統合し、現行要求への吸収を判定する文書）を分析文書とは別の種別として扱ったのはなぜか？
A. 分析文書は一次資料ごとに独立して書かれるため、同じ要求が別々のIDで重複する。突合表は複数の分析文書の候補を統合したうえで現行要求への吸収を判定するもので、性格が違う。`requirements-analysis/README.md` の「文書の位置づけ」の表に5つ目の種別として追加し、一覧も分析文書とは別の節に置いた。
（出典: 本日の作業から）

### Q. 領域ごとに5択（要求候補を採用・却下などの区分に振り分ける手順）を作らなかったのはなぜか？
A. 同じ候補が複数領域に跨るため、部分的に確定させると整合が取れなくなるため。実際、`PR-M-05`・`PR-M-06` は領域01から02へ、`ARF-M-03` は領域01から04へ、`PR-M-04`・`PR-M-12`・`PR-M-16`・`ARF-M-10` は領域02から04へ、`ARF-M-07` は領域03から04へ送られた。4領域すべての突合が終わってから5択を作る、という方針にした。
（出典: 本日の作業から）

### Q. 突合の順序を「再現実装 → 読解・学習 → 証拠・由来 → 成果物・外部連携」とした理由は何か？
A. 3つの基準で決めた。1つ目は実装に近い順（設計工程が完了し、次はソフトウェア実装のフェーズ0-1の永続化であるため）。2つ目は現行要求が具体的な順（`REQ-C05` は受入基準が明確だが、`REQ-C08`・`REQ-C09` は「最終的に」という語を含み曖昧であるため）。3つ目は依存の上流から扱う順（何を記録するかが決まってから版管理を設計するため）。
（出典: 本日の作業から）

### Q. 🕐（時期尚早）という判定記号を導入した理由は何か？
A. 領域04（成果物・外部連携）では、候補が現行要求に収まるかとは別に、今この段階で決める必要があるかを見る必要があった。`requirements.md` の第8章が `REQ-C08`〜`REQ-C11` の段階2以降を第2フェーズへ先送りしているためである。既存の ✅（現行要求に含まれる）／🔨（サブ要求として吸収）／🆕（新規）／⏳（保留）に加えて🕐（時期尚早）を設け、3件をそこへ分類した。今決めなくてよいものを5択に載せると、判断材料が乏しいまま選ぶことになるという考え方による。
（出典: 本日の作業から）

### Q. `ARF-M-02`（再現の3水準を利用者が選べるようにする候補）の判定はどう誤り、どう修正したか？
A. 要求文だけを読んで「新規性は選べる点にある」と判断し、⏳（保留）とした。しかし `requirements.md` 第0章を読むと、REF-10（ACM Artifact Badging が定める再現の3水準）の3水準がすでに全文で定義済みで、さらに56〜58行が「タイプBでは主に Reproducibility を目指す。公式実装を使わない選択では Replicability として区別し、成果物にどちらを目指したかを明記する」と定めていた。利用者が水準を選ぶ余地まで含まれていたため、判定を ✅ に訂正した。同じ REF-10 を根拠とする候補が、既に反映済みだったことになる。
（出典: 本日の作業から）

### Q. 候補の要求文と現行の要求文を並べるだけでは足りないとはどういうことか？
A. `ARF-M-02` の誤判定から得た教訓である。候補の要求文と現行の要求文を並べるだけでは足りず、現行の該当箇所（第0章、サブ要求の全文、実装単位、データモデル）を先に読む必要がある。領域02以降の突合はこの順序に変えた。
（出典: 本日の作業から）

### Q. 判定件数を本文の複数箇所に書いて直し漏れが起きたのはなぜか？
A. 突合表01で、判定の件数を集計表・論点の見出し・次に行うことの3箇所に書いていた。`ARF-M-02` の判定を訂正したとき、集計表は直したが、論点の見出し「保留5件の帰属先」と5節の「⏳ 2件（`AS-M-02`・`ARF-M-02`）」が古いまま残った。教訓として、件数は集計表だけが持ち、本文では候補IDで参照する方針にした。これは前日（2026-09-01）から続けている「状態は正本が1つだけ持つ」という考え方と同じである。領域02以降の突合表は冒頭に「件数は集計表だけが持つ」と明記した。
（出典: 本日の作業から）

### Q. 36件の要求候補を突合した結果、現行要求に収まらなかったのは何件だったか？
A. 6件だった。突合対象は36件（`PR-M-01`〜16、`ARF-M-01`〜13、`AR-M-01`〜03、`AS-M-01`〜04）。領域別の内訳は、01再現実装が15→13件中✅2・🔨4・🆕3、02読解・学習が16件中✅2・🔨6・🆕2、03証拠・由来が3件中✅0・🔨1・🆕1、04成果物・外部連携が7→6件中✅1・🔨2・🆕0。統合により4組8件が4件に減り、判定できなかったものが2件（工程モデル未確定）と1件（既存機能との突合が必要）、時期尚早と判定されたものが3件あった。
（出典: 本日の作業から）

### Q. 「候補が50件超溜まっている」「読解側は新規が多い」という事前の見立ては当たっていたか？
A. どちらも外れた。数えた時点では候補が多く見えたが、実際に突合すると大半は既存要求の言い換えだった。特に `REQ-C10-S01`〜`S04` は読解深度・実験条件・検証・主張区分を細かく定めており、読解領域の候補16件の多くを吸収した。`REQ-C09-S02`（深掘りキュー）も、関連論文・引用による影響・影響の時間変化を扱うため、年表や連鎖の提示といった候補を吸収した。要求定義がすでにかなり網羅的だったことが分かった。
（出典: 本日の作業から）

### Q. `requirements.md` の第8章の範囲指定が領域04の判定を単純化したのはなぜか？
A. `requirements.md` 823行が、`F-17`〜`F-20`、`B-12`〜`B-15`、`N-08`〜`N-10`（`REQ-C08`〜`REQ-C11` の段階2以降）を第2フェーズ以降へ先送りすると明示している。初期リリースに含まれる成果物系の項目は `F-11`（成果物ダウンロード、規約名 zip）だけである。このため領域04の候補7件は、そのほぼすべてが初期リリースの対象外にあたり、新規メイン要求は0件になった。範囲が明確に書かれていると、判定が機械的に済むことが分かった。
（出典: 本日の作業から）

### Q. この製品の現行要求にまったく無い領域として何が見つかったか？
A. 生成AIと人間の貢献の切り分けを記録する仕組みが無いことが見つかった。`git grep -n "生成AI\|CRediT\|貢献" -- docs/requirements.md` を実行した結果、「CRediT」（研究の貢献を役割語彙で表す標準）と「貢献」はいずれも0件、「生成AI」は4箇所あるがすべて論文の対象領域（`REQ-C03`）か回答主体（`REQ-C11`）としての言及だった。この製品は生成AIを使って読解と再現実装を支援するが、「この成果物のどこを生成AIが作り、どこを人間が作ったか」を記録しない。`B-13` は「生成条件とともに」双方向追跡すると定めるが、それは工程の記録であって貢献の記録ではない。
（出典: 本日の作業から）

### Q. 工程モデルが確定要件でないために判定できなかった候補は何か？
A. Y-01（読解工程の提示と完了記録）と `AR-M-03`（プロジェクト管理）の2件。どちらも「工程として提示し進捗を記録する」ことを求めるが、`REQ-C01` の実装単位は `F-02`（暫定）であり、`requirements.md` の第0章と第1.1節が工程モデル自体は確定要件ではないと明記している。確定していないものへ候補を当てられない、という判断でこの2件は判定を保留した。
（出典: 本日の作業から）

### Q. 突合の結果得られた新規メイン要求候補6件は何か？
A. X-01（01再現実装: 実行の再現条件、乱数種・試行回数・要約統計の記録）、`PR-M-08`（01: スケールダウン・プランナー、差分Δと仮定台帳）、`PR-M-09`（01: 再現スコープ定義書の生成）、`PR-M-13`（02読解・学習: 設計原理の抽象化と他ドメイン転用の提示）、`ARF-M-12`（02: 熟達段階に応じた支援の粒度調整）、`ARF-M-08`（03証拠・由来: 生成AIと人間の貢献の切り分け）の6件。いずれも要求文1行にもとづく判定であり、5択の前に全文を確認する必要がある。
（出典: 本日の作業から）

### Q. 候補間の統合はどのように行ったか？
A. 分析文書が独立して書かれた結果、同じ要求が別IDで重複していたため統合した。X-01 は `ARF-M-06` ＋ `AS-M-01`（乱数種・試行回数・要約統計の記録。機械学習の再現性チェックリストと Marek Rei という独立した2出典が同じことを求めていた）。X-02 は `ARF-M-05` ＋ `PR-M-10`（評価指標の抽出と、評価スクリプトの雛形生成。同じ工程の前段と後段）。Y-01 は `ARF-M-01` ＋ `PR-M-02`（読解を工程として提示し完了を記録。違いは段階の定義だけ）。Z-01 は `PR-M-04` ＋ `ARF-M-10`（関連研究の時系列的位置づけ。同じことを別の語で述べていた）。
（出典: 本日の作業から）

### Q. 統合しなかった候補はどれで、なぜ統合しなかったのか？
A. `ARF-M-04`（情報の充足検査）と `PR-M-09`（再現スコープ定義書）は向きが逆であるため統合しなかった。前者は「何が足りないか」を検査し、後者は「何をやるか」を定義する。`ARF-M-08`（貢献の帰属）と `AR-M-02`（公開前点検）も、生成AIの関与に触れる点は共通するが目的が違うため統合しなかった。
（出典: 本日の作業から）

## 開発の型の出現記録
このプロジェクトの開発ならではの「型」を記録する。前日の devlog（`docs/devlog/devlog-2026-09-01.md`）はこの会話中で先に生成済みだが、そこに記録された決定・つまずき・Git操作は本日の項目とは別の具体的な事案であるため、累積は合算せず本日分のみを記載する。

### 決定ログ（なぜその技術・設計を選んだか）
| 決定した事項 | 選んだ理由 | 却下した代替案 | 本日 |
|---|---|---|---|
| 突合表を分析文書とは別の種別として`requirements-analysis/README.md`へ追加 | 分析文書は一次資料ごとに独立して書かれ同じ要求が別IDで重複するが、突合表は複数文書の候補を統合し現行要求への吸収を判定するもので性格が違うため | 突合表を分析文書の一種として同じ節に置く | 本日1 |
| 領域ごとに5択を作らず4領域すべての突合が終わってから作る | 同じ候補が複数領域に跨るため、部分的に確定させると整合が取れなくなるため | 領域ごとに5択を作り順次確定させる | 本日1 |
| 突合の順序を「再現実装→読解・学習→証拠・由来→成果物・外部連携」とする | 実装に近い順、現行要求が具体的な順、依存の上流からという3基準による | 分析文書が作られた順や候補数が多い順で突合する | 本日1 |
| ✅／🔨／🆕／⏳に加え🕐（時期尚早）を判定記号として導入 | 第8章が`REQ-C08`〜`REQ-C11`の段階2以降を第2フェーズへ先送りしており、今決めなくてよい候補を5択に載せると判断材料が乏しいまま選ぶことになるため | 🕐を設けず⏳（保留）に含めて扱う | 本日1 |

### つまずき→解決の記録
| つまずき | 原因 | 解決方法 | 本日 |
|---|---|---|---|
| `ARF-M-02`を要求文だけで判定し誤った（⏳とすべきところを本来は✅） | 候補の要求文と現行の要求文を並べるだけで判定し、現行の該当箇所（第0章、サブ要求の全文）を先に読んでいなかった。実際にはREF-10の3水準が既に全文で定義され、利用者が水準を選ぶ余地まで含まれていた | 判定を✅に訂正。領域02以降は現行の該当箇所（第0章、サブ要求の全文、実装単位、データモデル）を先に読む順序に変更 | 本日1 |
| 判定件数を集計表・論点の見出し・次に行うことの3箇所に書き、`ARF-M-02`訂正時に集計表しか直せず古い件数が残った | 件数を本文の複数箇所に重複して書いていたため | 件数は集計表だけが持ち、本文では候補IDで参照する方針に変更。領域02以降の突合表は冒頭に「件数は集計表だけが持つ」と明記 | 本日1 |

### Git・環境操作の記録
| 操作 | コマンド/手順 | 効果 | 本日 |
|---|---|---|---|
| リポジトリ全体からCRediT・貢献関連の記述を検索 | `git grep -n "生成AI\|CRediT\|貢献" -- docs/requirements.md` | 「CRediT」「貢献」が0件、「生成AI」の4箇所も貢献の記録とは無関係と判明し、生成AIと人間の貢献を切り分ける記録の仕組みが現行要求に無いことを確認 | 本日1 |

## 語彙・用語リスト（プロジェクト固有）
| 用語 | 定義 |
|---|---|
| 突合表（crosswalk） | 複数の分析文書の要求候補を統合し、現行要求への吸収を領域ごとに判定した文書。5択（要求候補を採用・却下などの区分に振り分ける手順）の入力になる |
| ✅／🔨／🆕／⏳／🕐 判定記号 | 突合表で使う判定記号。✅は現行要求に含まれる、🔨はサブ要求として吸収、🆕は新規、⏳は保留、🕐は時期尚早（現行要求への収まりとは別に、その段階で決める必要が無いもの）を表す |
| X-01・X-02・Y-01・Z-01 | 突合の過程で複数候補を統合した際に付けた暫定ID。X-01は`ARF-M-06`＋`AS-M-01`、X-02は`ARF-M-05`＋`PR-M-10`、Y-01は`ARF-M-01`＋`PR-M-02`、Z-01は`PR-M-04`＋`ARF-M-10`をそれぞれ統合したもの |
| CRediT | 研究の貢献を役割語彙で表す標準。誰が何を担ったかを記録する仕組み |
| Repeatability / Reproducibility / Replicability | ACM Artifact Badging（REF-10）が区別する再現の3水準。`requirements.md`第0章が全文で定義済み |

## 未解決の疑問と翌日の開発計画
- 未解決の疑問:
  - 工程モデルを確定させるか。`REQ-C01`の実装単位が`F-02`（暫定）のまま止まっている。Y-01・`AR-M-03`の判定の前提であり、要求の話を超えて製品設計の根幹に関わる
  - 新規メイン要求候補6件の全文確認が未実施。突合はすべて要求文1行にもとづく。`ARF-M-02`で1件、要求文だけの判定を誤った実績がある
  - `AS-M-02`（評価コードの検証）が既存のサニティ階段（タイプBの「オラクル満点」の段）で足りるか
  - X-01と`REQ-C10-S02`の関係。記録する項目（乱数、試行回数、評価指標、エラーバー、計算資源）は同一だが向きが違う。あちらは論文に書かれた条件を読み取り、こちらは自分の実行条件を記録する。独立した要求か、対になるサブ要求かが未確定
  - 🕐（時期尚早）と判定した3件の扱い。今回の5択に載せるか、`worknotes/pending-*.md`へ移すか
  - `PR-M-16`（解説資料の生成）と帳票編の非該当判断の関係。採用すれば、前日（2026-09-01）の決定記録`decision-batch-report-not-applicable.md`の「見直す条件」に該当する
- 翌日の計画:
  - 工程モデルを確定させるかを決める
  - 新規メイン要求候補6件の全文を確認する
  - 5択を作る（`requirements-update-workflow.md`）
  - 承認後、`requirements-change-proposal.md`を改訂し、`requirements.md`をv0.3へ上げる
  - 並行して、ソフトウェア実装のフェーズ0-1（`projects` / `papers`のPostgreSQL永続化）を進める

## 画面キャプチャ記録
本日は画面キャプチャの共有なし
