# 開発ログ: paper-repro — 2026-09-11（JST）

## メタ情報
- 日付（JST）: 2026-09-11
- 本日の作業範囲: **要件定義の保留8件のうち1件を解除した日である。** `requirements.md` 第10.2節の「第9章の対応表と `product-design.md`・`roadmap.md` の突き合わせ」を対象とした。実測の過程で、解除条件が現状の文書では判定できないことが分かり、**条件そのものを書き直してから解除した。** 第8章「初期リリース範囲」の2つの一覧を `product-design.md` 第6章の判定と一致させ、`REQ-C12` の実装単位 `F-21`・`B-16` に初期リリースの判定を与えた。コミット2件。設計文書と実装コードには触れていない。
- 記録の範囲についての注意: 本ログは2026年9月11日20時台から23時台の claude.ai セッションの記録である。同日のコミットは `0e83c1f`（20時17分）と `d0e2188`（22時48分）の2件で、いずれも `docs/requirements.md` と `docs/product-design.md` のみを変更している。
- 参照したソース URL:
  - 本日は該当なし

## Q&A 知識カード
その日に理解したことを、問いと答えの形で残す。各カードは単独で読めるように書く。

### Q. 保留の解除条件が「判定できない」状態になることがあるのはなぜか？
A. **条件を書いた時点で、照合先の文書がその形式を持つかを確認していないためである。** paper-repro の保留「第9章の対応表と `product-design.md`・`roadmap.md` の突き合わせ」の解除条件は、実装単位が `roadmap.md` のフェーズ計画に現れるかを確認することだった。2026年9月11日に実測したところ、**`roadmap.md` に実装単位ID（`F-xx`・`B-xx`・`N-xx`）は1件も現れず、要求IDも `REQ-C01` の1件だけだった。** 同書はフェーズ名と確認画面で進捗を記述する方針を明示しており、ID照合という判定方法そのものが成り立たない。**解除条件は、照合先が実際にその情報を持つことを確かめてから書く。**
（出典: 本日の作業から。コミット `0e83c1f`）

### Q. 判定できない解除条件を残したまま保留を解除すると、何が起きるのか？
A. **満たしていない条件を満たしたことにして解除した、という記録が残る。** 2026年9月11日、`roadmap.md` 側が判定不能だと分かった時点で、先に解除条件を書き直し、外した理由を第10.2節へ注記してから解除した。**条件を直さずに第8章の作業だけ進めると、記録の上では「解除条件を確認した」ことになり、後から追跡できない。** paper-repro では2026年9月9日から11日にかけて「確かめずに完了と書く」誤りが3度出ており、同じ形の再発を避けるための手順である。
（出典: 本日の作業から。コミット `0e83c1f`）

### Q. 要求・実装単位・設計箇所・初期リリース範囲の対応は、どの文書が持っているのか？
A. **3つの文書が別々の向きの対応を持つ。**

| 文書 | 持っている対応 |
|---|---|
| `requirements.md` 第9章 | 要求 → 実装単位（33行） |
| `product-design.md` 第6章 | 実装単位 → 設計箇所、および初期リリースの判定（47件） |
| `requirements.md` 第8章 | 初期リリースに含める実装単位と先送りする実装単位 |

`roadmap.md` はフェーズ名と確認画面で進捗を記述しており、IDの対応は持たない。**`roadmap.md` へ同じ対応を持たせると、同じ情報が3箇所に増えて、どれが正しいか分からなくなる。**
（出典: 本日の作業から。コミット `0e83c1f`）

### Q. 照合先の文書が古いとき、その判定をそのまま正本へ写してよいか？
A. **写してよい範囲と、新しい判断が要る範囲を分ける。** `product-design.md` は v0.3（2026年9月3日06時49分）で止まっており、以後 `requirements.md` には要求6件（`REQ-C04-S03`・`REQ-C07-S02`・`REQ-C07-S03`・`REQ-C09-S05`・`REQ-C10-S05`・`REQ-C12`）と実装単位2件（`F-21`・`B-16`）が加わった。2026年9月11日に実測したところ、**新設された実装単位は `F-21`・`B-16` の2件だけで、第6章の既存45件の判定はそのまま使えた。** 新しい判断が要ったのは2件だけである。**「古い」と分かった時点で全部を疑うのではなく、どこが古いかを実測する。**
（出典: 本日の作業から。コミット `d0e2188`）

### Q. `product-design.md` のヘッダーが「準拠する要件 v0.2」のままだったのはなぜか？
A. **v0.3.1 へ追随したコミットが、本文の1箇所だけを直してヘッダーを直さなかったためである。** 2026年9月3日のコミット `b588f19` は題名が "follow the requirements version to v0.3.1" だが、変更したのは本文中の「要件 v0.3 第1.1節」という1行だけで、ヘッダーの「準拠する要件: `requirements.md` v0.2」はそのまま残った。**コミットの題名が作業の完了を保証しない。** 2026年9月11日に v0.3.1 へ直した。
（出典: 本日の作業から。コミット `0e83c1f`）

### Q. 第8章「初期リリース範囲」に実装単位が21件も現れていなかったのはなぜか？
A. **第8章が、代表的な実装単位だけを挙げる書き方をしていたためである。** 書き直し前の「初期リリースに含める」は `F-01`・`F-02`〜`F-06`・`F-15`・`F-10`・`F-11`・`B-01`〜`B-04`・`B-05`/`B-06` の15件、「先送りする」は `F-17`〜`F-20`・`B-12`〜`B-15`・`N-08`〜`N-10` だけだった。2026年9月11日の実測では、第9章が参照する実装単位のうち **21件**（`F-07`・`F-08`・`F-09`・`F-12`・`F-13`・`F-14`・`F-16`・`F-21`、`B-07`〜`B-11`・`B-16`、`N-01`〜`N-07`）がどちらの一覧にも現れなかった。**一覧に無い実装単位は、含めるとも先送りするとも決まっていない状態である。**
（出典: 本日の作業から。コミット `d0e2188`）

### Q. 「先送りする」と「最小形のみ」を同じ行に書くと、何が読み取れなくなるのか？
A. **最小形が初期リリースに入ることが読み取れなくなる。** 書き直し前の第8章は「`F-17`〜`F-20`、`B-12`〜`B-15`、`N-08`〜`N-10`（`REQ-C08`〜`REQ-C11` の段階2以降）→ 第2フェーズ以降」と1行で書いていた。一方 `product-design.md` 第6章は `F-17` を「最小形のみ」、`B-12`〜`B-15` を「最小形のみ／以降は第2フェーズ」としている。括弧書きの「段階2以降」を読めば整合するが、**見出しが「初期リリースで先送りする」なので、最小形が初期リリースに含まれることが見出しと矛盾して見える。** 2026年9月11日に、最小形を「含める」側へ、それを超える部分を「先送りする」側へ分けた。
（出典: 本日の作業から。コミット `d0e2188`）

### Q. `F-21` と `B-16` を初期リリースに含めると判断したのはなぜか？
A. **`REQ-C12` が、初期リリースの受け入れ基準の前提を作る要求だからである。** `REQ-C12`（再現スコープの定義）は、再現する要素を4〜6個に絞り、**各構成要素へ完了判定条件（サニティチェック）を紐づける**ことを求める。初期リリースの受け入れ基準は「オラクル満点確認」と「識別力の高い2タスクで論文値と ±5 に収まる」ことであり、**どのタスクを選ぶかと、何をもって完了とするかが `REQ-C12` の出力に依存する。** 先送りすると、何を再現するかが決まらないまま spec を書く形になる。なお `arc-screen-list.md` 5.5節の保留は `F-21` の画面を `S-03-01` と `S-05-01` のどちらに置くかという配置の問題で、初期リリースに含めるかどうかとは別である。
（出典: 本日の作業から。コミット `d0e2188`）

### Q. 保留を1件解除したのに、保留の総数が8件のままなのはなぜか？
A. **解除の過程で、担当工程の違う保留が1件新しく立ったためである。** `F-17` と `B-12`〜`B-15` の「最小形」が、2026年9月8日に加わったサブ要求（`REQ-C07-S02`・`REQ-C07-S03`・`REQ-C04-S03`・`REQ-C09-S05`・`REQ-C10-S05`）で足りるかは決まっていない。**初期リリースに含めるかどうかは第8章で決着したので、残るのは最小形の中身である。** 中身は表示項目と保持項目で決まるため、担当工程は `F-17` が画面編の設計、`B-12`〜`B-15` が外部インタフェース編の設計とした。**要件定義の工程内で決められる保留は、これで非機能要求の1件だけになった。**
（出典: 本日の作業から。コミット `d0e2188`）

### Q. 第8章の「含める」が15件から40件へ増えたのは、初期リリースの範囲が広がったということか？
A. **範囲が広がったのではなく、決まっていなかった21件に判定が付いた結果である。** 判定の出所は `product-design.md` 第6章で、そこでの初期リリース判定は2026年9月3日以前に置かれたものである。**したがって「40件になった」ことは、範囲が妥当かどうかを何も語らない。** 第6章の判定は要求が23件だった頃の設計に基づいており、その後6要求が増えている。**数が合ったことと、範囲として妥当であることは別の問いである。**
（出典: 本日の作業から）

### Q. 2つの文書の一覧が一致しているかを、目視以外でどう確かめるのか？
A. **両方の一覧から実装単位IDの集合を作り、差集合が空であることを確かめる。** 2026年9月11日は、`requirements.md` 第8章の2つの一覧と `product-design.md` 第6章の判定列から集合を作り、「第6章が第2フェーズなのに第8章が含める」「第6章が初期リリースなのに第8章に無い」「第6章が第2フェーズなのに第8章の先送りに無い」の3方向を検査した。**`F-02`〜`F-06` のような範囲表記を展開しないと、範囲に含まれる実装単位が集合から漏れる。**
（出典: 本日の作業から）

### Q. 範囲表記の展開を誤ると、検証の結果はどう壊れるのか？
A. **一致しているものを食い違いとして報告する。** 2026年9月11日の1回目の検証で、`B-02`・`B-03`・`B-13`・`B-14`・`F-03`・`F-04`・`F-05`・`N-02`・`N-03`・`N-04` の10件が「第6章が初期リリース側なのに第8章に無い」と出た。原因は、範囲表記が `` `F-02`〜`F-06` `` のようにバッククォートを挟む形で書かれており、展開の正規表現が `F-02〜F-06` だけを想定していたことだった。**バッククォートを除いてから展開し直すと、食い違いはゼロになった。** 検証の道具そのものが誤ることがあるため、**食い違いが出たときは、まず道具を疑う。**
（出典: 本日の作業から）

## 開発の型の出現記録
このプロジェクトの開発ならではの「型」を記録する。**本日分のみを記載する。過去の devlog は「本日N」の形式で累積を引き継いでいないため、累積更新には前日までの devlog を貼ってください。**

### 決定ログ（なぜその技術・設計を選んだか）
| 決定した事項 | 選んだ理由 | 却下した代替案 | 本日 |
|---|---|---|---|
| 保留の解除条件から `roadmap.md` を外す | 同書に実装単位IDが1件も無く、ID照合で判定できないため。要求→実装単位は第9章が、実装単位→設計箇所は `product-design.md` 第6章が持っており、`roadmap.md` へ持たせると3箇所目の重複になる | `roadmap.md` へ実装単位IDの列を足して照合できるようにする | 本日1 |
| 解除条件の書き直しを、第8章の作業より先に行う | 判定できない条件を残したまま解除すると、満たしていない条件を満たしたことにする記録が残るため | 第8章を直してから条件を整える、条件を直さずに解除する | 本日1 |
| `F-21`・`B-16` を初期リリースに「含む」と判定する | `REQ-C12` が各構成要素へ完了判定条件を紐づける要求であり、受入基準の「オラクル満点確認」と「識別力の高い2タスク」の選定がその出力に依存するため | 第2フェーズ以降とする、最小形のみとする | 本日1 |
| `F-17` と `B-12`〜`B-15` の「最小形」の中身を、新しい保留として立てる | 初期リリースに含めるかは第8章で決着しており、残るのは表示項目と保持項目で、画面編と外部インタフェース編の設計でしか決められないため | 要件定義で中身まで決める、保留にせず放置する | 本日1 |
| 第8章で、最小形を「含める」側へ、それを超える部分を「先送りする」側へ分ける | 1行に書くと、見出しと括弧書きが矛盾して見え、最小形が初期リリースに入ることが読み取れないため | 従来どおり1行にまとめる | 本日1 |
| `product-design.md` を v0.4 へ上げる | 第6章へ `F-21`・`B-16` の2行を追加し、内容が変わったため | 版を据え置く | 本日1 |
| `requirements.md` の版は v0.3.1 のまま据え置く | 版の付け方の方針そのものが決まっておらず、この場で決めるべきではないため | v0.3.2 または v0.4 へ上げる | 本日1 |

### つまずき→解決の記録
| つまずき | 原因 | 解決方法 | 本日 |
|---|---|---|---|
| 保留の解除条件が、現状の文書では判定できなかった | 条件を書いた時点で、`roadmap.md` がID表記を持つかを確認していなかった | 実測してID表記が無いことを示し、解除条件から `roadmap.md` を外して理由を注記した | 本日1 |
| 照合先の `product-design.md` が5日前（2026年9月3日）で止まっていた | 9月7日から8日に要求6件と実装単位2件が増えたが、設計文書が追随していなかった | 新設された実装単位が `F-21`・`B-16` の2件だけであることを実測し、その2件にだけ新しい判断を下した | 本日1 |
| `product-design.md` のヘッダーが「準拠する要件 v0.2」のままだった | 2026年9月3日の `b588f19` が本文の1箇所だけを v0.3.1 へ直し、ヘッダーを直さなかった | v0.3.1 へ訂正した。**コミットの題名が作業の完了を保証しない** | 本日1 |
| 第9章が参照する実装単位のうち21件が、第8章のどちらの一覧にも無かった | 第8章が代表的な実装単位だけを挙げる書き方だった | 第6章の判定を写して2つの一覧を全面的に書き直した | 本日1 |
| `F-17` と `B-12`〜`B-15` が、第6章では「最小形のみ」なのに第8章では先送り側にあった | 「段階2以降」という括弧書きだけで区別しており、見出しと食い違っていた | 最小形と、それを超える部分を別の行へ分けた | 本日1 |
| 突き合わせの検証で、一致しているものを食い違い10件として誤検出した | 範囲表記が `` `F-02`〜`F-06` `` とバッククォートを挟む形で書かれており、展開の正規表現が想定していなかった | バッククォートを除いてから展開し直した。**食い違いが出たときは、まず検証の道具を疑う** | 本日1 |
| 索引外に残る旧件数を、2026年9月10日に「6文書」と報告していた | 走査の範囲が浅かった | 再実測すると**37箇所・13文書**だった | 本日1 |

### Git・環境操作の記録
| 操作 | コマンド/手順 | 効果 | 本日 |
|---|---|---|---|
| コミット `0e83c1f` | `git commit` | 保留1件目の解除条件から `roadmap.md` を外し、第8章の「第3章の11件」を12件へ直し、`product-design.md` の準拠行を v0.3.1 へ訂正した | 本日1 |
| コミット `d0e2188` | `git commit` | 第8章の2つの一覧を第6章の判定と一致させ、`F-21`・`B-16` に判定を与え、保留1件目を解除して新しい保留を1件立てた | 本日1 |
| 第9章の対応表の実測 | `awk` で第9章を切り出し、`\| REQ` で始まる行を数える | 33行であることと、参照される実装単位の集合を確認した | 本日1 |
| `roadmap.md` のID実測 | `grep -oE "\b(F\|B\|N)-[0-9]{2}\b" roadmap.md \| sort -u \| wc -l` | 0件であることを確認し、解除条件が判定不能だと示した | 本日1 |
| 新設された実装単位の特定 | `git show b588f19:docs/requirements.md` の実装単位IDと現在のIDを `comm -13` で比較 | 増えたのが `F-21`・`B-16` の2件だけであることを確認した | 本日1 |
| 第8章と第6章の突き合わせ検証 | Python で両方から実装単位IDの集合を作り、3方向の差集合を検査 | 食い違いゼロを確認した。範囲表記はバッククォートを除いてから展開する | 本日2 |
| push 後のリモート照合 | `git show origin/main:<path>` を取得して手元の生成物と `diff` | リモートの内容が生成物と完全一致することを確認した | 本日2 |

## 語彙・用語リスト（プロジェクト固有）
このプロジェクトで出た固有用語を、毎回定義ごと書く。

| 用語 | 定義 |
|---|---|
| 解除条件の判定可能性 | 保留の解除条件が、照合先の文書の記述形式で実際に判定できるかどうか。paper-repro では `roadmap.md` に実装単位IDが無いため、ID照合を求める解除条件が判定不能だった |
| 初期リリース判定 | `product-design.md` 第6章が実装単位ごとに持つ「含む」「最小形のみ」「第2フェーズ以降」の3値。`requirements.md` 第8章の2つの一覧は、この判定と一致していなければならない |
| 最小形のみ | 実装単位のうち、限定した機能だけを初期リリースへ含め、それを超える部分を第2フェーズ以降とする判定。paper-repro では `F-17`（解決待ちパネル）と `B-12`〜`B-15`（`pending` の最小形）が該当する |
| `F-21` 再現スコープ定義書 | `REQ-C12` の実装単位。再現する要素を4〜6個に絞り、データセット・モデル・損失・評価の4区分で表示し、スコープ外を除外理由つきで並べ、各構成要素へ完了判定条件を紐づける。2026年9月11日に初期リリースへ含めると判定した |
| `B-16` 再現スコープ管理 | `REQ-C12` の実装単位のうちバックエンド側。スコープ定義書を生成し、4区分と除外理由、完了判定条件を保存する。2026年9月11日に初期リリースへ含めると判定した |
| 3つの対応表 | 要求→実装単位を持つ `requirements.md` 第9章、実装単位→設計箇所と初期リリース判定を持つ `product-design.md` 第6章、初期リリースに含める実装単位を持つ `requirements.md` 第8章。`roadmap.md` はIDの対応を持たない |
| 範囲表記の展開 | `` `F-02`〜`F-06` `` のような表記から `F-02`・`F-03`・`F-04`・`F-05`・`F-06` の集合を作る処理。バッククォートを除いてから展開しないと、範囲に含まれる実装単位が漏れる |

## 未解決の疑問と2026年9月12日の計画
- 未解決の疑問:
  - **第8章「初期リリースに含める」が40件になったが、範囲として妥当かは確かめていない。** 判定の出所である `product-design.md` 第6章は2026年9月3日以前の設計に基づいており、その後 `REQ-C04-S03`・`REQ-C07-S02`・`REQ-C07-S03`・`REQ-C09-S05`・`REQ-C10-S05`・`REQ-C12` の6要求が加わっている。**数が合ったことと、範囲として妥当であることは別の問いである。**
  - `F-21`・`B-16` を初期リリースへ含めるという判定は、2026年9月11日22時台に下したものである。判断の理由は `requirements.md` 第10.2節に残してあり、覆す場合は第8章の2行、`product-design.md` 第6章の2行、第10.2節の理由を直せばよい。
  - `requirements.md` の版の付け方が決まっていない。v0.3.1 は2026年9月3日に付けられ、その後9月7日から8日に要求が6件増えても据え置かれ、9月11日の保留解除でも据え置いた。
  - 索引外に残る旧件数は、再実測で**37箇所・13文書**だった。`arc-datamodel-list.md` 66行目の免責（ER図・CRUD図・論理属性定義の「17エンティティ」は `DM-E18` を含まない）で説明できるものと、単に古いものが混ざっている。
  - 要件定義の工程内で決められる保留は、非機能要求 `N-01`〜`N-10` の1件だけになった。
- 2026年9月12日の計画:
  - **第8章「初期リリースに含める」40件を、範囲として妥当か見直す。** あわせて `F-21`・`B-16` の判定を読み直す。判定を写した今だからこそ見えるようになった問いである。
  - `DIF-06`・`DIF-13` が決着済みかを `requirements.md` 第8章と `REQ-C06` の展開内容と突き合わせて確認し、決着していれば `requirements-update-workflow.md` 5.5節の「保留」を解除する。
  - 対応欄の仕様ID化を、グループ05（12業務）とグループ04（21業務）へ進める。グループ05は `REQ-C06` の33仕様との突き合わせを要する。グループ04が最大である。
  - 索引の外に残った旧件数37箇所・13文書を、免責で説明できるものと古いものへ切り分ける。
  - `behaviors/` へ追加15業務の説明を書くかどうかを判断する。
  - **`requirements.md` 第10.2節の保留（非機能要求 `N-01`〜`N-10`）は、規模の見積もりが要るため必須項目としない。** `N-01`〜`N-05` と `N-07` の6件について、要求文と受入基準を書けるだけの根拠が一次資料にあるかを確認する作業である。

## 画面キャプチャ記録
| # | 何の画面か | 結果・状態 |
|---|---|---|
| — | 本日は画面キャプチャの共有なし | PowerShell の実行結果はテキストで共有された。`0e83c1f` の push が `16291ce..0e83c1f main -> main`、`d0e2188` の push が `0e83c1f..d0e2188 main -> main` で成功したこと、差分がそれぞれ23行追加/4行削除、50行追加/28行削除であることが含まれる |
