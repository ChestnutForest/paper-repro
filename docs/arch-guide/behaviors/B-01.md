# B-01 プロジェクトとポートフォリオ

- 対応フロー: `BF-01`
- 対応要求: `REQ-C09`, `REQ-C09-S01`, `REQ-C09-S02`
- USDM状態: 仕様未展開

| ID・種別 | 利用者／事前条件／トリガー | 入力（種別） | 基本シナリオ | 代替・例外 | 出力（種別）／事後条件 | 要求／ルール |
| --- | --- | --- | --- | --- | --- | --- |
| `B-01-001` 機能 | system／保存領域に接続可能／一覧表示 | 検索条件（画面） | 条件に合うプロジェクトと`phase/status/course`を返す | 0件は空一覧。取得失敗は理由と再試行手段を返す | 一覧（画面）／状態は変更しない | `REQ-C09`; `BR-04`,`BR-07` |
| `B-01-002` 利用 | 利用者／対象が存在／再開操作 | project ID（エンティティ） | 最新の保存地点と成果物を読み、対応画面を開く | 対象なしは一覧へ戻す。破損は復元候補を示す | 復元済み作業状態（画面）／保存データ不変 | `REQ-C09-S01`; `BR-11` |
| `B-01-003` 利用 | 利用者／一覧表示済み／条件入力 | 論文・状態・日付（画面） | 条件を適用し該当一覧を表示する | 条件不正は項目別に示す | 絞込一覧（画面）／状態不変 | `REQ-C09-S01`; `BR-07` |
| `B-01-004` 利用 | 利用者／対象と保存先を確認可能／入出力操作 | 対象、形式、ファイル（画面・ファイル） | 影響を確認し、バックアップ／復元／移行出力を行う | 互換性不明は実行前に停止。途中失敗は元データを保持 | 移行物または復元結果（ファイル・エンティティ）／履歴保存 | `REQ-C09-S01`; `BR-04`,`BR-11` |
| `B-01-005` 利用 | 利用者／対象が存在／削除操作 | project ID、確認（画面） | 対象と影響を表示し、再確認後に削除する | 未確認は中止。関連物が残る場合は削除しない | 削除結果（画面・イベント）／監査履歴保存 | `REQ-C09-S01`; `BR-01`,`BR-11` |
| `B-01-006` 機能 | system／根拠データあり／更新イベント | 学習履歴・関連資料（エンティティ） | 更新差分、復習対象、深掘り状態を集約する | 不明な関連は候補のまま保持 | ポートフォリオ（エンティティ）／出典時点を保存 | `REQ-C09`,`REQ-C09-S02`; `BR-04`,`BR-10` |

