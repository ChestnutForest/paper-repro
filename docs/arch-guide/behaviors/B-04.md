# B-04 読解・学習・仕様化

- 対応フロー: `BF-04`
- 対応要求: `REQ-C02`, `REQ-C04`, `REQ-C04-S01`, `REQ-C04-S02`, `REQ-C05-S01`, `REQ-C06`〜`REQ-C11`
- USDM状態: `REQ-C01`関連部分を除き仕様未展開

| ID・種別 | 利用者／事前条件／トリガー | 入力（種別） | 基本シナリオ | 代替・例外 | 出力（種別）／事後条件 | 要求／ルール |
| --- | --- | --- | --- | --- | --- | --- |
| `B-04-001` 利用 | 利用者／原文保存済み／表示・注釈 | 原文、版、位置（ファイル・画面） | 原文を表示し注釈・メモ・位置を保存する | 版変更時は未対応注釈を再確認対象にする | 読書状態（エンティティ）／再開可能 | `REQ-C09-S01`; `BR-04`,`BR-11` |
| `B-04-002` 機能 | system／対象概念と根拠あり／追加説明要求 | 概念、理解状態（画面） | 難易度を段階化し根拠位置へのリンクを返す | 根拠が無ければ説明を確定情報にしない | 説明（画面）／一次資料へ戻れる | `REQ-C02`; `BR-10` |
| `B-04-003` 機能 | system／主張・数式等を選択済み／課題要求 | 対象、条件（エンティティ） | 例、反例、境界、手計算、確認点を分けて提示 | 複数解法を許し、一例で一般化しない | 課題（画面・エンティティ） | `REQ-C04-S01`; `BR-08`,`BR-10` |
| `B-04-004` 機能 | system／回答または条件変更あり／再計算 | 入力、回答、条件（画面） | 結果を再計算し定義・計算・観察・解釈・範囲別に返す | 計算不能は原因を示し、文字列一致だけで誤答にしない | 比較・フィードバック（画面）／履歴保存 | `REQ-C04-S01`; `BR-07`,`BR-10` |
| `B-04-005` 利用 | 利用者／根拠箇所選択済み／自己説明保存 | 説明、理解状態、疑問（画面） | 自分の言葉と次の深掘りを原文へ関連付ける | 流暢さだけで理解済みにしない | 自己説明（エンティティ）／訂正履歴保存 | `REQ-C04-S02`; `BR-10` |
| `B-04-006` 機能 | system／原文と抽出結果あり／草案生成 | 原文、抽出、生成条件（エンティティ） | 根拠位置と未確認を伴うspec草案を生成する | 生成失敗は理由を返し、原文と既存編集を保持する | spec草案（ファイル・エンティティ）／未承認状態 | `REQ-C04`,`REQ-C07`,`REQ-C08`; `BR-06`,`BR-10`,`BR-12` |
| `B-04-007` 利用 | 利用者／草案または空のspecあり／編集 | spec、根拠、編集内容（画面） | 人が根拠と内容を修正し、未確認を保持する | 草案生成失敗時も手編集可能。保存失敗は旧版を保持 | 編集済みspec（ファイル・エンティティ）／未承認状態 | `REQ-C04`,`REQ-C07`,`REQ-C08`; `BR-06`,`BR-10` |
| `B-04-008` 利用 | 利用者／論文と実装の版あり／対応付け | 論文位置、コード位置（エンティティ） | 一致、実装詳細、省略、版差等を分類して双方向保存 | 取得不能・検査不能を推測で埋めない | 対応表（エンティティ）／採用理由を保持 | `REQ-C05-S01`,`REQ-C08`; `BR-10` |
| `B-04-009` 利用 | 利用者／重要記述を選択／構造化操作 | 原文、実験、根拠（エンティティ） | 前提、帰結、範囲、条件、主張区分、分類主体を保存 | 未報告・推定・外部確認済みを区別。LLM単独確定不可 | 証拠台帳（エンティティ）／訂正可能 | `REQ-C10-S01`,`S02`,`S04`; `BR-08`,`BR-10` |
| `B-04-010` 利用 | 利用者／関連候補あり／キュー操作 | 候補、信号、関心（エンティティ） | 優先度、理由、探索状態を承認・変更する | 人気等1指標で自動採用しない。有償資料を代理取得しない | 深掘りキュー（エンティティ） | `REQ-C09-S02`; `BR-08`,`BR-10` |
| `B-04-011` 利用 | 利用者／疑問あり／質問・回答記録 | 質問、回答主体、原文、公開範囲（画面） | 回答を出典・日時・主体とともに非確定情報として保存 | 外部送信は別の明示操作。取得不能は未回答のまま | 質問台帳（エンティティ） | `REQ-C11`; `BR-10`,`BR-11` |
| `B-04-012` 利用 | 利用者／重要事象発生／登録・解決・保留 | 種類、対象、理由（画面） | `waiting_approval`にし、解決または保留理由を記録して`idle`へ戻す | 理由なし・種類不正は拒否。phaseは変えない | pending・承認履歴（エンティティ） | `REQ-C06`,`REQ-C07`,`REQ-C10`; `BR-09` |
| `B-04-013` 利用 | 利用者／specとpendingを確認済み／承認 | spec版、course、理由（画面） | specを固定し`B-02-005`でコース別遷移を行う | 未解決が残る場合の可否は要求未決。別工程からは拒否 | spec版・承認履歴・遷移（エンティティ・イベント） | `REQ-C01-R2.10`〜`.60`,`REQ-C06`; `BR-09` |
