# B-06 レポート・成果物・共有

- 対応フロー: `BF-06`
- 対応要求: `REQ-C01`, `REQ-C05`, `REQ-C07`〜`REQ-C11`, `REQ-C09-S01`, `REQ-C09-S03`, `REQ-C10-S04`
- USDM状態: `REQ-C01-R2.70`を除き仕様未展開

| ID・種別 | 利用者／事前条件／トリガー | 入力（種別） | 基本シナリオ | 代替・例外 | 出力（種別）／事後条件 | 要求／ルール |
| --- | --- | --- | --- | --- | --- | --- |
| `B-06-001` 機能 | system／照合または読解成果あり／レポート表示 | 論文値、再現値、根拠、未確認（エンティティ） | 値、条件差、支持・反証、由来を並べて表示する | 読解コースは実装値なしでも表示。欠落を隠さない | レポート（画面）／判断前状態 | `REQ-C07`,`REQ-C08`,`REQ-C10`; `BR-07`,`BR-10` |
| `B-06-002` 利用 | 利用者／レポート表示済み／注記保存 | 判断、理由、未解決（画面） | 差異と採用根拠を記録し、LLM判断を訂正可能にする | 根拠なしの確定は警告。矛盾は削除しない | 最終注記（エンティティ）／履歴保存 | `REQ-C07`,`REQ-C10-S04`; `BR-10` |
| `B-06-003` 機能 | system／出力対象確定／出力操作 | spec、台帳、コード、結果、レポート（ファイル・エンティティ） | manifestと由来を付けて移行可能なパッケージを生成する | readingは読解成果だけで生成可。失敗時は`done`にしない | zip等（ファイル）／生成履歴保存 | `REQ-C01-R2.70`,`REQ-C05`,`REQ-C08`,`REQ-C09-S01`; `BR-11`,`BR-12` |
| `B-06-004` 機能 | system／パッケージ生成成功／完了イベント | 生成結果（イベント） | 成功履歴を保存し`scoring`から`done`へ遷移する | 保存失敗・生成失敗は`scoring`に留める | `TR-07`（イベント）／`done/idle` | `REQ-C05`,`REQ-C08`; `BR-12` |
| `B-06-005` 利用 | 利用者／共有可能成果物あり／公開設定 | 対象、範囲、プレビュー（画面） | 機密、個人情報、秘密、権利、引用、範囲を確認する | 1項目でも未確認なら送信せず非公開を維持 | 公開承認（エンティティ）／送信前 | `REQ-C09-S03`,`REQ-C11`; `BR-01`,`BR-08`,`BR-11` |
| `B-06-006` 利用 | 利用者／`B-06-005`承認済み／最終操作 | 成果物、送信先、公開範囲（画面・ファイル） | 明示操作で送信し、主体・日時・取消条件を記録する | 送信失敗は公開済みにしない。取消制約を表示 | 共有結果・履歴（外部応答・エンティティ） | `REQ-C09-S03`,`REQ-C11`; `BR-02`,`BR-11` |

