# 要求仕様書（USDM 形式）

- 対象プロダクト: `paper-repro`
- 版: v0.1（`REQ-C01` のみ完全展開。他は要求と理由まで）
- 作成日: 2026年8月26日
- 記法: USDM（Universal Specification Describing Manner）
- 記法の出典: [`references.md`](references.md) の **REF-17**（AFFORDD T2研究会「USDM 小冊子 基礎編」ver 1.3, 2016）
- 記法の採用範囲: [`references-usdm-ipa.md`](references-usdm-ipa.md) 第1節
- 仕様ごとの工程の進捗: [`traceability-matrix.md`](traceability-matrix.md)
- USDM を含む派生開発プロセス XDDP: [`references-usdm-ipa.md`](references-usdm-ipa.md) 1.5節

## 1. 本書の位置づけ

### 1.1 なぜ USDM で書き直すか

[`requirements.md`](requirements.md) v0.2 の要求文は、**何を作るか**を述べているが、
USDM が要求に担わせる3つの役割を果たしていない。

| 要求の3つの役割（REF-17 2.2.2） | v0.2 の要求文の状態 |
|---|---|
| 求められている**振る舞い**を見せて動きを感じさせる | 「提供するものとする」など、名詞形・静的な表現が多い |
| 求められている**範囲**を示す | どこから始まりどこで終わるかが曖昧 |
| 要求に含まれる**全ての動詞と目的語**を見せる | 動詞が省かれ、仕様を引き出す手がかりが少ない |

USDM は、要求を**振る舞い**として動詞形で書き、その動詞ひとつひとつに仕様を対応させる。
これにより**仕様の漏れに気づきやすくなる**（REF-17 3.1）。

### 1.2 他の文書との関係

| 文書 | 関係 |
|---|---|
| [`requirements.md`](requirements.md) | **確定要求の正本（v0.2）。** 本書は同じ要求を USDM 形式へ書き直したもの |
| [`requirements-change-proposal.md`](requirements-change-proposal.md) | 要求文と受入基準の出所 |
| [`arch-guide/arc-datamodel-framework.md`](arch-guide/arc-datamodel-framework.md) | 仕様から導く論理データモデル、4工程成果物、レビュー基準 |
| [`arch-guide/arc-datamodel.md`](arch-guide/arc-datamodel.md) | フェーズ0の物理仕様（DDL・状態遷移表） |
| [`roadmap.md`](roadmap.md) | 仕様を画面で確認する手段 |

> **要求の正本は当面 [`requirements.md`](requirements.md) v0.2 である。**
> 本書が全要求を展開し終えるまで、両方を維持する。展開が済んだ時点で、
> どちらを正本とするかを判断する（第13章）。

### 1.3 記法の約束

USDM の基本フォーマット（REF-17 4.1）に従い、次の5項目で書く。

| 項目 | 内容 |
|---|---|
| **カテゴリ名** | 要求のグループ名と記号 |
| **要求** | 振る舞いを動詞形で。要求 ID を付ける |
| **理由** | その要求が生じた背景 |
| **説明** | 要求への補足 |
| **仕様** | 仕様グループごとに、具体的な処理内容。仕様 ID を付ける |

**ID の付け方**（REF-17 4.2.5・4.4.4）

- 要求 ID: 既存の `REQ-Cxx` をそのまま使う。**新しい体系を作らない。**
  番号を振り直すと、決定台帳3冊と分析文書19本の追跡が切れるため。
- 下位要求 ID: `REQ-Cxx-Rn`（例: `REQ-C01-R1`）
- 仕様 ID: `REQ-Cxx-Rn.mm`（例: `REQ-C01-R1.10`）。10刻みで振り、間に挿入できるようにする

**避ける表現**（REF-17 4.5.3〜4.5.5）

- 「等」「etc」— 現時点で確定できることは書き切る。未確定なら合意時期を書く
- **否定表現** — 「〜しない」の裏に隠れた条件分岐がある。`else` を必ず書く
- **ペースト作文** — 一部だけ違う仕様をコピーで作らない。差異が読み取れなくなる

---

## 2. REQ-C01 読解と再現実装の二経路【完全展開】

**フェーズ0で実装中のため、本要求のみ仕様まで展開する。**

### 2.1 上位要求

| カテゴリ | 経路選択（`COURSE`） |
|---|---|
| **要求 `REQ-C01`** | システムは利用者に**開始時に**読解コースか再現実装コースを**選ばせ**、選んだ経路に応じて工程を**進め**、作業の途中でも経路を**切り替えさせ**、切り替えても既存の成果物を**保持する**ものとする。 |
| **理由** | 論文を読み解く力を育てたい利用者と、動くコードを得たい利用者では、必要な工程が違う。片方を主・他方を従にすると、従にした側の利用者が使えない。また、読んでいるうちに実装したくなる（逆もある）ため、途中で乗り換えられないと最初の選択が重荷になる。 |
| **説明** | 経路は `Project.course` として保持する。**NOT NULL・既定値なし**とし、未選択の状態を存在させない。切り替えは何度でも可能。実装単位は `F-15`（画面）と `F-02`（工程表示）。 |

**動詞の数を数える。** 「選ばせる」「進める」「切り替えさせる」「保持する」の4つ。
8個未満なので、**分割しないでよい**（REF-17 4.2.2）。ただし振る舞いの局面が
「開始時」「進行中」「切り替え時」に分かれるため、**時系列分割**（同 4.2.4）で
3つの下位要求へ分ける。局面ごとに仕様の性質が異なるためである。

### 2.2 下位要求 `REQ-C01-R1` 開始時に経路を選ぶ

| 項目 | 内容 |
|---|---|
| **要求** | 利用者が新規作成を**始めた**とき、システムは2つの経路とそれぞれで到達できる成果物を**示し**、どちらか一方を**選ばせ**、選択と論文の URL を受け取って**プロジェクトを作る**ものとする。 |
| **理由** | 選ばないまま作れると、利用者は自分がどちらの経路にいるか分からないまま作業を始めてしまう。最初に選ばせることで、以降の画面が何を見せるべきかも決まる。 |
| **説明** | 経路の選択肢は `reading`（読解・学習）と `reproduction`（再現実装）の2つ。3つ目は当面設けない。 |

**＜経路の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R1.10` | 新規作成の画面に、`reading` と `reproduction` の2つを、同時に見える形で並べて表示する。 |
| `REQ-C01-R1.20` | 各経路の下に、その経路で到達できる成果物を1行で表示する。`reading` は「spec と仮定台帳」、`reproduction` は「実行可能なノートブックと照合レポート」とする。 |
| `REQ-C01-R1.30` | 後から切り替えられる旨を、選択肢の近くに表示する。 |
| `REQ-C01-R1.40` | 表示する文言は `next-intl` の翻訳キーで参照し、`ja` `en` `zh-TW` の3ファイルすべてに登録する。 |

**＜経路の選択＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R1.50` | 初期状態ではどちらも選択されていない状態にする。 |
| `REQ-C01-R1.60` | 一方を選ぶと、他方の選択は解除される。両方を同時に選択できない。 |
| `REQ-C01-R1.70` | どちらも選択されていない間は、作成の操作を実行できない状態にする。 |

**＜プロジェクトの作成＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R1.80` | `POST /api/v1/projects` へ、論文の URL と選択した経路の2つを必須の項目として送る。 |
| `REQ-C01-R1.90` | 経路を欠いた要求を受け取った場合、HTTP 422 を返し、プロジェクトを作らない。 |
| `REQ-C01-R1.100` | 経路が `reading` と `reproduction` のいずれでもない値だった場合も、HTTP 422 を返し、プロジェクトを作らない。 |
| `REQ-C01-R1.110` | 作成に成功した場合、`phase` を `created`、`status` を `idle` として保存し、HTTP 201 と作成したプロジェクトを返す。 |

> **`REQ-C01-R1.90` と `.100` を分けて書いた理由。**
> 「経路が正しくなければ 422」と1行で書くと、**欠落と不正値のどちらも指すのか読み取れない。**
> USDM が禁じる曖昧さにあたるため、条件ごとに行を分けた（REF-17 4.5.4）。

### 2.3 下位要求 `REQ-C01-R2` 経路に応じて工程を進める

| 項目 | 内容 |
|---|---|
| **要求** | 承認ゲートで工程を進めるとき、システムはそのプロジェクトの経路を**参照し**、`reproduction` なら実装以降の工程へ**進め**、`reading` なら読解の工程に**留め**、留めた理由を利用者に**示す**ものとする。 |
| **理由** | 読解コースの利用者に実装工程を見せても、使わない画面が並ぶだけで迷わせる。ただし「進めない」とだけ表示すると故障と区別がつかないため、経路が理由であることを示す必要がある。 |
| **説明** | 工程の遷移そのものの規則は [`arch-guide/arc-datamodel.md`](arch-guide/arc-datamodel.md) 3.4 に従う。本要求は、そこへ経路による条件を重ねる。 |

**＜経路の参照＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R2.10` | 承認ゲート②（`spec:finalize`）の実行時に、対象プロジェクトの `course` を読み取る。 |
| `REQ-C01-R2.20` | 工程の遷移表には経路の条件を含めない。経路による判定は、ゲートのエンドポイント側で行う。 |

**＜再現実装コースの進行＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R2.30` | `course` が `reproduction` の場合、`phase` を `reading` から `implementing` へ進める。 |

**＜読解コースの滞留＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R2.40` | `course` が `reading` の場合、`phase` を `reading` のまま保つ。 |
| `REQ-C01-R2.50` | 同じ場合に HTTP 400 を返し、応答の本文に「読解コースのため実装工程へ進まない」旨と、現在の経路の値を含める。 |
| `REQ-C01-R2.60` | 画面に、進めなかった理由として経路を表示する。故障や権限の不足と区別できる文言にする。 |

**＜読解コースの終端＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R2.70` | `course` が `reading` の場合でも、成果物（spec と仮定台帳）の出力は実行できる状態にする。 |

### 2.4 下位要求 `REQ-C01-R3` 経路を切り替えても成果物を保持する

| 項目 | 内容 |
|---|---|
| **要求** | 利用者が経路の切り替えを**指示した**とき、システムは新しい経路を**記録し**、切り替えた時刻を**残し**、既存の成果物をすべて**保持したまま**、切り替え後の経路に応じた画面を**表示する**ものとする。 |
| **理由** | 切り替えのたびに作業が失われるなら、利用者は切り替えを避ける。避けられる機能は無いのと同じである。保持を保証して初めて、途中で乗り換えられるという価値が成立する。 |
| **説明** | 保持の対象は spec、仮定台帳、注釈、自己説明、承認履歴、コスト記録、サニティ実行結果、照合結果。切り替えは `POST /api/v1/projects/{project_id}/course` で行う。 |

**＜切り替えの記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R3.10` | 新しい経路を `Project.course` へ保存する。 |
| `REQ-C01-R3.20` | 切り替えた時刻を `Project.course_changed_at` へ保存する。 |
| `REQ-C01-R3.30` | 切り替えを承認履歴（`Approval`）へ1件記録し、切り替え前と切り替え後の経路の両方を含める。 |
| `REQ-C01-R3.40` | 切り替え前と同じ経路が指定された場合も、要求として受理し、HTTP 200 と現在のプロジェクトを返す。この場合は `course_changed_at` と承認履歴を更新しない。 |

**＜成果物の保持＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R3.50` | 切り替えの前後で、spec、仮定台帳、注釈、自己説明、承認履歴、コスト記録の各件数と内容を同一に保つ。 |
| `REQ-C01-R3.60` | `reproduction` から `reading` へ切り替えた場合、サニティ実行結果と照合結果を削除せず、参照できる状態で残す。 |
| `REQ-C01-R3.70` | `reading` から `reproduction` へ切り替えた場合、既存の spec と仮定台帳をそのまま引き継ぎ、実装工程の入力として使う。 |
| `REQ-C01-R3.80` | 切り替えによって `phase` と `status` を変更しない。 |

**＜切り替え後の表示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C01-R3.90` | 切り替え後、画面に表示している経路の名称を新しい経路へ更新する。 |
| `REQ-C01-R3.100` | 切り替え後、`reproduction` を選んだ場合は実装工程への導線を表示し、`reading` を選んだ場合はその導線を表示しない。 |

> **`REQ-C01-R3.40` を書いた理由。**
> 「同じ経路が指定されたら」という条件は、要求文にも受入基準にも現れない。
> しかし実装では必ず起き、書かないと**時刻と履歴が無意味に更新される**か、
> **エラーを返して利用者を戸惑わせる**かのどちらかになる。
> USDM の「隠れた IF 文」にあたる（REF-17 4.5.4）。

### 2.5 動詞と仕様の対応（漏れの検査）

上位要求の動詞ひとつひとつに、仕様が対応しているかを確かめる（REF-17 3.1 ①）。

| 上位要求の動詞 | 下位要求 | 対応する仕様グループ | 仕様の数 |
|---|---|---|---|
| 選ばせる | `R1` | 経路の提示（4）、経路の選択（3）、プロジェクトの作成（4） | 11 |
| 進める | `R2` | 経路の参照（2）、再現実装コースの進行（1）、読解コースの滞留（3）、読解コースの終端（1） | 7 |
| 切り替えさせる | `R3` | 切り替えの記録（4）、切り替え後の表示（2） | 6 |
| 保持する | `R3` | 成果物の保持（4） | 4 |
| | | **合計** | **28** |

**28仕様。対応の無い動詞は無い。**

> 「切り替える」と「保持する」は、どちらも `R3` に属する。
> 切り替えという1つの操作に対して、**記録する側面**と**失わせない側面**があり、
> 仕様グループとしては分けたほうが漏れに気づきやすいためである。

**数の検証。** 仕様の定義数は次で数える。表の行頭に限定しないと、
本文中の参照まで数えてしまう（実際 31 件になる）。

```powershell
(Select-String -Path docs\requirements-usdm.md -Pattern "^\| ``REQ-C01-R\d\.\d+``" -AllMatches).Matches.Count
```

---

## 3. REQ-C03 論文の取り込み【完全展開】

**展開の順序で最初に位置づけた要求である**（第13章）。論文が入らなければ他の工程が始まらない。

### 3.1 上位要求

| カテゴリ | 論文の取り込み（`INTAKE`） |
|---|---|
| **要求 `REQ-C03`** | 利用者が論文を**指定した**とき、システムは入手元に応じたアダプタで本文とメタ情報を**取り込み**、版と公開状態を**記録し**、取り込めなかった項目を**そのまま空にして示す**ものとする。 |
| **理由** | 最初は arXiv だけで足りるが、後から PDF や出版社ページを足すたびに中核を書き換えるのでは持たない。また、不明を推測で埋めると、後の判断が誤った前提の上に載る。 |
| **説明** | 初期リリースで実装するアダプタは arXiv のみ。取り込みの具体的な手順は [`arch-guide/interfaces/IF-01.md`](arch-guide/interfaces/IF-01.md) が定める。**本書は取り込みの振る舞いを定め、外部インタフェースの仕様を重複して書かない。** |

**動詞の数を数える。** 「取り込む」「記録する」「示す」の3つ（「指定する」はトリガであり、
システムの振る舞いではない）。8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「入手元の判別」「取得の実行」「保存」に分かれるため、
**時系列分割**（同 4.2.4）で3つへ分ける。

⚠️ **3つ目は既存サブ要求 `REQ-C03-S01` が担う。**
[`requirements-decisions/batch-07-options.md`](requirements-decisions/batch-07-options.md) 選択肢3に従い、
既存サブ要求はIDを保ったまま下位要求と同じ階層へ置く。**新しく `R3` を作らない。**

---

### 3.2 下位要求 `REQ-C03-R1` 入手元を判別してアダプタを選ぶ

| 項目 | 内容 |
|---|---|
| **要求** | 利用者が論文を指定したとき、システムは入力から入手元を**判別し**、対応するアダプタを**選び**、対応するものが無い場合は取り込まずに**その旨を示す**ものとする。 |
| **理由** | 入手元ごとに取得の手順も返る形式も違う。中核の処理が入手元を知っていると、追加のたびに中核を書き換えることになる。 |
| **説明** | アダプタは共通のインタフェースを実装し、中核は入手元を知らない。受入基準の「新しい分野・入手元を中核フロー変更なしに追加できる」はこの構造で満たす。 |

**＜入手元の判別＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R1.10` | 入力として、URL、識別子（arXiv ID・DOI）、アップロードされた PDF のいずれかを受け取る。 |
| `REQ-C03-R1.20` | 入力の形式から入手元を判別する。判別の結果を、取り込んだ論文の属性として保存する（`REQ-C03-S01`）。 |
| `REQ-C03-R1.30` | 入力が arXiv の URL または arXiv ID の場合、arXiv アダプタを選ぶ。 |
| `REQ-C03-R1.40` | 版を含む arXiv ID（`2501.01234v2` など）と、版を含まない ID を区別して判別する。 |

**＜アダプタの選択＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R1.50` | アダプタは、本文・メタ情報・版・公開状態を返す共通のインタフェースを実装する。 |
| `REQ-C03-R1.60` | 中核の取り込み処理は、個々のアダプタの実装を参照しない。アダプタの追加によって中核を変更しない。 |
| `REQ-C03-R1.70` | 初期リリースで実装するアダプタは arXiv のみとする。 |

**＜未対応の入手元＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R1.80` | 対応するアダプタが無い入手元だった場合、取り込みを実行せず、未対応である旨を利用者へ示す。 |
| `REQ-C03-R1.90` | 未対応の場合、入力を破棄せず、利用者が手入力で続けられる経路を示す。 |
| `REQ-C03-R1.100` | 入手元を判別できなかった場合と、判別できたが未対応だった場合を、区別して示す。 |

> **`R1.80` と `R1.100` を分けて書いた理由。**
> 「対応していなければその旨を示す」と1行で書くと、**判別できなかったのか、判別できたが
> アダプタが無いのかを利用者が区別できない。** 前者は入力の誤り、後者は製品の制約であり、
> 次にとるべき行動が違う。

---

### 3.3 下位要求 `REQ-C03-R2` 本文とメタ情報を取り込む

| 項目 | 内容 |
|---|---|
| **要求** | アダプタを選んだあと、システムは外部インタフェースを通じて本文とメタ情報を**取得し**、取得の結果を「正常」「該当なし」「失敗」へ**判別し**、失敗した場合は代替の手段を**示す**ものとする。 |
| **理由** | 取得は必ず成功するとは限らない。成功・該当なし・失敗を区別しないと、論文が存在しないのか通信に失敗したのかが分からず、利用者が次に何をすべきか判断できない。 |
| **説明** | arXiv の取得手順は [`arch-guide/interfaces/IF-01.md`](arch-guide/interfaces/IF-01.md) が定める。**本書は判別と後続の振る舞いだけを定め、リクエストの組み立てや応答の項目は重複して書かない。** |

**＜取得の実行＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R2.10` | 選んだアダプタの手順に従って取得する。arXiv の場合は `IF-01` の 2.3節に従う。 |
| `REQ-C03-R2.20` | 取得は非同期で実行し、実行中も利用者が他の操作を行える状態にする（`BR-12`）。 |
| `REQ-C03-R2.30` | 保存期間内に同じ対象を取得済みの場合、再取得せず保存済みの結果を使う（`IF-01` 2.2節）。 |

**＜結果の判別＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R2.40` | 取得の結果を「正常」「該当なし」「入力が不正」「通信の失敗」の4つへ判別する。 |
| `REQ-C03-R2.50` | HTTP の状態コードだけで判別しない。arXiv はエラーを HTTP 200 の正常な形式で返す（`IF-01` 1.4節）。 |
| `REQ-C03-R2.60` | 「該当なし」と「入力が不正」を区別する。前者は論文が存在せず、後者は利用者の入力に誤りがある。 |

**＜本文の取得＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R2.70` | 本文は HTML 版を優先して取得する。取得できない場合に PDF から抽出する。 |
| `REQ-C03-R2.80` | 本文を取得できなかった場合も、メタ情報だけで取り込みを完了できる状態にする。 |

**＜失敗時の振る舞い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-R2.90` | 「入力が不正」の場合、再試行せず、誤りの内容を利用者へ示す（`BR-13`）。 |
| `REQ-C03-R2.100` | 「通信の失敗」の場合、`BR-13` のリトライ規則に従う。制限内の失敗を異常終了として扱わない。 |
| `REQ-C03-R2.110` | リトライ後も失敗した場合、利用者による手入力での代替を示す（`IF-01` 2.4節）。 |
| `REQ-C03-R2.120` | 取得に失敗しても、既に到達した工程を巻き戻さない（`BR-13`）。 |

---

### 3.4 既存サブ要求 `REQ-C03-S01` 取り込んだ情報を保存する

⚠️ **本節は USDM の下位要求ではなく、5択を経て確定した既存サブ要求である。**
[`requirements-decisions/batch-07-options.md`](requirements-decisions/batch-07-options.md) 選択肢3に従い、
IDを保ったまま同じ階層へ置き、仕様を `REQ-C03-S01.10` と付番する。

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは論文の入手元、識別子、版、カテゴリー、取得日時、公開状態、採択先とその履歴を保存し、**不明な情報を推測で補わない**ものとする。 |
| **受入基準**（同上） | 入手元、版、カテゴリー、取得日時、公開状態を履歴付きで保存し、カテゴリーや査読状態だけで真偽を判定せず、不明を `unknown` として扱える。 |

**＜保存する項目＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-S01.10` | 入手元、識別子、版、カテゴリー、取得日時、公開状態、採択先を保存する。 |
| `REQ-C03-S01.20` | 公開状態と採択先は履歴として保存し、後から変化を追える形にする。 |
| `REQ-C03-S01.30` | 版を指定して取得した場合と、最新版を取得した場合を区別して保存する。 |
| `REQ-C03-S01.40` | 同じ論文の異なる版を、別の記録として保存する。上書きしない。 |

**＜不明の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-S01.50` | 取得できなかった項目を `unknown` として保存する。**推測で補わない。** |
| `REQ-C03-S01.60` | `unknown`（取得できなかった）と「値が空である」（取得できたが空だった）を区別して保存する。 |
| `REQ-C03-S01.70` | `unknown` の項目を画面に表示するとき、空欄ではなく不明であることが分かる形で示す。 |

**＜自動判定の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C03-S01.80` | カテゴリー、査読状態、採択先のいずれか1つだけで、論文の内容の真偽を判定しない。 |
| `REQ-C03-S01.90` | 公開状態は事実として保存し、信頼性の評価と分けて扱う（`REQ-C07-S01`）。 |

> **`S01.60` を独立させた理由。**
> 「不明を `unknown` として扱う」だけでは、**取得できなかったのか、取得した結果が空だったのかを
> 区別しない実装が成立してしまう。** 前者は再取得で埋まる可能性があり、後者は埋まらない。
> 後の判断が変わるため、区別を仕様として明示した。

---

### 3.5 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 入手元に応じたアダプタで**取り込む** | `R1.10`〜`R1.100`、`R2.10`〜`R2.80` |
| 版と公開状態を**記録する** | `S01.10`〜`S01.40` |
| 取り込めなかった項目を**そのまま空にして示す** | `R2.90`〜`R2.120`、`S01.50`〜`S01.70` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 初期対象と拡張対象を識別する | `R1.20`、`R1.70` |
| 新しい分野・入手元を中核フロー変更なしに追加できる | `R1.50`、`R1.60` |
| 入手元・版・カテゴリー・取得日時・公開状態を履歴付きで保存 | `S01.10`、`S01.20` |
| カテゴリーや査読状態だけで真偽を判定しない | `S01.80`、`S01.90` |
| 不明を `unknown` として扱える | `S01.50`〜`S01.70` |

⚠️ **未確認事項。** 取り込みの API エンドポイントは [`product-design.md`](product-design.md) を
確認していないため、仕様に書いていない。`REQ-C01-R1.80` が `POST /api/v1/projects` を使うのに対し、
**論文の取り込みが同じエンドポイントか別かを確認したうえで追記する。**

---

## 4. REQ-C05 再現実装の支援範囲【完全展開】

**初期リリースの中核である。** [`requirements.md`](requirements.md) 第8章の受け入れ基準「複数回実行の平均が論文値と ±5 に収まる」は
本要求が扱う範囲にあたる。

### 4.1 上位要求

| カテゴリ | 再現実装（`REPRO`） |
|---|---|
| **要求 `REQ-C05`** | システムは公式実装を**読み解き**、コードとノートブックを**生成し**、隔離した環境で**実行し**、サニティチェックを**通し**、論文の値と**照合する**ものとする。 |
| **理由** | 手元で動かなければ再現したとはいえない。ただし第三者のコードをそのまま実行するのは危険なため、隔離を挟む。サニティを通す前の照合は、誤った成功を通してしまう。 |
| **説明** | 本要求が指す「再現」の水準は第0章の定義による。初期リリースはタイプB（学習なし・公式実装あり）が対象で、主に Reproducibility を目指す。実行環境の隔離条件は `REQ-C06` が定める。**本章は隔離の仕組みを重複して書かない。** |

**動詞の数を数える。** 「読み解く」「生成する」「実行する」「通す」「照合する」の5つ。
8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「読解」「生成」「実行と検査」「照合」に分かれるため、
**時系列分割**（同 4.2.4）で4つへ分ける。

⚠️ **うち3つは既存サブ要求が担う。**
[`requirements-decisions/batch-07-options.md`](requirements-decisions/batch-07-options.md) 選択肢3に従い、
既存サブ要求はIDを保ったまま下位要求と同じ階層へ置く。

| 局面 | 担当 |
|---|---|
| 公式実装の読解 | **`REQ-C05-S01`**（既存サブ要求） |
| コードとノートブックの生成 | `REQ-C05-R1`（新設） |
| 隔離実行とサニティチェック | `REQ-C05-R2`（新設）＋ **`REQ-C05-S02`**（既存） |
| 論文値との照合 | `REQ-C05-R3`（新設）＋ **`REQ-C05-S02`・`REQ-C05-S03`**（既存） |

---

### 4.2 既存サブ要求 `REQ-C05-S01` 論文と実装を双方向に対応付ける

⚠️ **本節は USDM の下位要求ではなく、5択を経て確定した既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは論文の節、主張、数式、図表または擬似コードを、公式実装のリポジトリ、版、コミット、タグ、ファイル、シンボル、行範囲、Notebookセル、設定および配布物へ双方向に対応付ける。対応関係は、一致、実装詳細、論文での省略、版差、意図的変更、バグ候補、原因不明、入手不能または検査不能として記録し、**論文と実装の双方を保持する**。どちらを採用するかは対象実験との版整合、証拠および利用者の承認理由とともに保存し、**一方を自動的に常時優先しない**ものとする。 |

**＜対応付けの記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S01.10` | 論文側の対象を、節・主張・数式・図表・擬似コードのいずれかとして記録する。 |
| `REQ-C05-S01.20` | 実装側の対象を、リポジトリ・版・コミット・タグ・ファイル・シンボル・行範囲・Notebookセル・設定・配布物のいずれかとして記録する。 |
| `REQ-C05-S01.30` | 対応付けから、論文側と実装側のどちらへも移動できる状態にする。 |
| `REQ-C05-S01.40` | 実装側の版を特定する情報（コミット・タグ）を必ず記録する。版を欠いた対応付けを保存しない。 |

**＜対応関係の分類＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S01.50` | 対応関係を、一致・実装詳細・論文での省略・版差・意図的変更・バグ候補・原因不明・入手不能・検査不能の9種へ分類する。 |
| `REQ-C05-S01.60` | 分類を確定できない場合、原因不明として記録する。**推測で他の分類を割り当てない。** |
| `REQ-C05-S01.70` | 公式実装の公開状態を、ソース検査可能・モデルおよびAPIのみ・非公開・取得不能・版不明へ分ける。 |

**＜採用の判断＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S01.80` | 論文と実装が食い違う場合、**両方を保持する。** 片方を削除しない。 |
| `REQ-C05-S01.90` | どちらを採用するかは利用者が決める。**システムが一方を常に優先しない。** |
| `REQ-C05-S01.100` | 採用の判断とともに、対象実験との版整合・証拠・承認理由を保存する。 |
| `REQ-C05-S01.110` | 採用の判断を後から訂正できる。訂正の履歴を保持する。 |

---

### 4.3 下位要求 `REQ-C05-R1` コードとノートブックを生成する

| 項目 | 内容 |
|---|---|
| **要求** | システムは読解した内容と再現の設定にもとづき、実行可能なコードとノートブックを**生成し**、生成に用いた前提を**添え**、利用者が編集できる形で**渡す**ものとする。 |
| **理由** | 生成したものをそのまま信じて実行すると、誤りに気づく機会がない。何を前提に生成したかが添えられていれば、実行前に人間が検査できる。 |
| **説明** | 初期リリースの成果物は Colab 用の `.ipynb`。環境のピン留め・サニティ段・推論・照合を含む構成とする（`B-07`）。 |

**＜生成の入力＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R1.10` | 生成の入力として、対応付け（`REQ-C05-S01`）、仮定台帳、差分Δ（`REQ-C05-S03`）を用いる。 |
| `REQ-C05-R1.20` | 入力に `unknown` の項目が含まれる場合、その項目を用いた生成を行わず、利用者へ確認を求める。 |

**＜生成の内容＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R1.30` | 生成するノートブックに、実行環境の版を固定する記述を含める。 |
| `REQ-C05-R1.40` | 生成するノートブックに、サニティチェックの各段を実行する記述を含める。 |
| `REQ-C05-R1.50` | 生成するノートブックに、論文値との照合を行う記述を含める。 |
| `REQ-C05-R1.60` | 生成したコードの各部分に、対応する論文箇所への参照を添える。 |

**＜生成後の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R1.70` | 生成物を利用者が編集できる状態で渡す。編集を禁じない。 |
| `REQ-C05-R1.80` | 利用者が編集した場合、生成時点の版と編集後の版の両方を保持する。 |

---

### 4.4 下位要求 `REQ-C05-R2` 隔離した環境で実行しサニティを通す

| 項目 | 内容 |
|---|---|
| **要求** | システムは生成または取得したコードを、隔離した環境で**実行し**、サニティチェックの各段を順に**通し**、通らなかった段で**止める**ものとする。 |
| **理由** | サニティを通す前に照合すると、動いていないコードが偶然出した値を「再現できた」と判定してしまう。段を飛ばせる経路があると、その保証が崩れる。 |
| **説明** | 隔離の条件は `REQ-C06` が定める。**本節は隔離の仕組みではなく、サニティの順序と停止の振る舞いを定める。** |

**＜実行の前提＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R2.10` | ホスト環境での直接実行を行わない。**例外を設けない。** |
| `REQ-C05-R2.20` | 実行前に、シークレットが生成物へ含まれていないことを検査する。 |
| `REQ-C05-R2.30` | 初期リリースの実行環境は CPU のみ、ネットワーク遮断とする（[`requirements.md`](requirements.md) 第8章）。 |

**＜サニティの順序（タイプB）＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R2.40` | タイプBのサニティを、データ実測 → 抽出 → オラクル満点 → 異常系0点 → E2E → 照合の順に実行する。 |
| `REQ-C05-R2.50` | **オラクル満点の段は、評価コードが正しく動くことを確かめる段である。** 正解を与えたとき満点にならなければ、評価側に誤りがある。 |
| `REQ-C05-R2.60` | 異常系0点の段は、誤った入力に対して0点となることを確かめる。満点と0点の両方を確かめないと、評価が入力を見ていない場合を検出できない。 |

**＜停止の振る舞い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R2.70` | いずれかの段が通らなかった場合、後続の段を実行しない。 |
| `REQ-C05-R2.80` | 通らなかった段を飛ばして照合へ進む操作を、画面にも API にも設けない。 |
| `REQ-C05-R2.90` | 各段の結果を pass または fail として記録し、fail の理由を添える。 |
| `REQ-C05-R2.100` | 実行のログ・標準出力・生成画像を、利用者が目視できる形で残す。 |

---

### 4.5 既存サブ要求 `REQ-C05-S02` 実行の再現条件を記録する

⚠️ **本節は既存サブ要求である**（第5バッチ選択肢3で確定）。

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは再現実行について、乱数種、試行回数、各試行の結果および要約統計を記録し、単一実行の結果を「単一実行」と明示するものとする。失敗した試行および途中で止めた試行も記録に残すものとする。 |

**＜記録する項目＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S02.10` | 実行ごとに乱数種を記録する。同じ種で再実行できる状態にする。 |
| `REQ-C05-S02.20` | 試行回数と、各試行の結果を個別に記録する。 |
| `REQ-C05-S02.30` | 平均と標準偏差を要約統計として算出し記録する。 |
| `REQ-C05-S02.40` | 失敗した試行と、途中で止めた試行も記録に残す。**成功した試行だけを残さない。** |

**＜単一実行の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S02.50` | 試行回数が1回の場合、結果に「単一実行」と明示する。 |
| `REQ-C05-S02.60` | 成果物に載せる数値へ、試行回数を必ず併記する。 |

**＜選択的報告の抑止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S02.70` | 複数の試行から最良値だけを取り出す操作に対し、注意を表示する。 |
| `REQ-C05-S02.80` | 最良値を採用した場合、その旨を記録に残す。**平均と区別できる形にする。** |

---

### 4.6 既存サブ要求 `REQ-C05-S03` 論文設定からの差分Δを残す

⚠️ **本節は既存サブ要求である**（第5バッチ選択肢3で確定）。

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは論文設定から変更した設定を差分Δとして残し、各行に変更点、変更理由、期待される性能差および出典を持たせ、仮定台帳へ転記するものとする。性能差を断定できない場合は不明と記録し、推測値を書かないものとする。 |

**＜差分Δの記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S03.10` | 論文設定から変更した項目を、1件ごとに独立した行として記録する。 |
| `REQ-C05-S03.20` | 各行に、変更点・変更理由・期待される性能差・出典の4つを持たせる。 |
| `REQ-C05-S03.30` | **変更した設定を1件も暗黙にしない。** 記録の無い変更を許さない。 |

**＜不明の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S03.40` | 期待される性能差を断定できない場合、不明と記録する。**推測値を書かない。** |
| `REQ-C05-S03.50` | 不明と、変更しても性能差が生じないことを区別して記録する。 |

**＜仮定台帳との関係＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-S03.60` | 差分Δの各行を仮定台帳へ転記する。 |
| `REQ-C05-S03.70` | 照合の結果が論文値と食い違った場合、差分Δの各行を原因の候補として参照できる状態にする。 |

---

### 4.7 下位要求 `REQ-C05-R3` 論文の値と照合する

| 項目 | 内容 |
|---|---|
| **要求** | システムはサニティを通した実行の結果を、論文から抽出した値と**並べ**、差を**示し**、判定の根拠となる条件を**添える**ものとする。 |
| **理由** | 数値だけを並べても、それが一致といえるかは判断できない。何回実行したか、どの設定を変えたかが添えられて初めて、差の意味が読める。 |
| **説明** | 判定は複数回実行の平均に対して行う（第0章）。試行回数と標準偏差の併記を条件とする。 |

**＜論文値の抽出＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R3.10` | 論文の主要数値を、出典（図表番号）とともに抽出する。 |
| `REQ-C05-R3.20` | 抽出できなかった数値を `unknown` とし、推測で補わない。 |

**＜照合の実行＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R3.30` | 再現値と論文値を並べ、差を算出する。 |
| `REQ-C05-R3.40` | **判定は複数回実行の平均に対して行う。** 各回すべてが範囲に収まることを求めない。 |
| `REQ-C05-R3.50` | 平均の取り方（単純平均・重み付き平均）を検算し、論文と同じ方法を用いる。 |
| `REQ-C05-R3.60` | サニティを通していない実行の結果を照合の対象としない。 |

**＜結果の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C05-R3.70` | 照合の結果に、試行回数・標準偏差・差分Δを併記する。 |
| `REQ-C05-R3.80` | サチュレーション側とクラッシュ側のタスクを区別して示す。 |
| `REQ-C05-R3.90` | 一致・不一致の判定を色で示す場合、色だけに頼らず文言も添える。 |

---

### 4.8 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 公式実装を**読み解く** | `S01.10`〜`S01.110` |
| コードとノートブックを**生成する** | `R1.10`〜`R1.80` |
| 隔離した環境で**実行する** | `R2.10`〜`R2.30`、`R2.100` |
| サニティチェックを**通す** | `R2.40`〜`R2.90` |
| 論文の値と**照合する** | `R3.10`〜`R3.90`、`S02.10`〜`S02.80`、`S03.10`〜`S03.70` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| ホスト直接実行を禁止 | `R2.10` |
| 隔離 | `R2.30`（詳細は `REQ-C06`） |
| 制限 | `R2.30` |
| シークレット分離 | `R2.20` |
| サニティ通過後に照合できる | `R2.70`、`R2.80`、`R3.60` |

⚠️ **未確認事項。** `REQ-C06`（実行言語の段階拡張と承認ゲート）が定める隔離の条件と、
本章 `R2.10`〜`R2.30` の境界。**`REQ-C06` の展開時に重複の有無を検査する。**

---

## 5. REQ-C02 前提知識の段階的説明【完全展開】

**読解経路の入口にあたる要求である。** `REQ-C01` は読解・学習と再現実装を同等の主要経路と定めており、
本要求は前者の基盤を担う。

### 5.1 上位要求

| カテゴリ | 学習支援（`LEARN`） |
|---|---|
| **要求 `REQ-C02`** | 利用者が理解できない語や数式に**出会った**とき、システムはその語の説明を**示し**、求めに応じて説明を**深め**、説明の根拠となる一次資料の箇所へ**戻す**ものとする。 |
| **理由** | 前提知識を利用条件にすると、学びたい人ほど使えなくなる。説明を読んで納得した気になるだけでは理解に至らないため、必ず原典へ戻せる必要がある。 |
| **説明** | 工程モデル（第1.1節）が定める **Scope 段階**が、論文の取り込み後に「この論文を読むのに何が足りないか」を評価し、**結果を本要求へ渡す。** 本章はその受け取り側を含む。 |

**動詞の数を数える。** 「示す」「深める」「戻す」の3つ（「出会う」はトリガであり、
システムの振る舞いではない）。8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「説明の提示」「説明の深化」「一次資料への復帰」に分かれるため、
**時系列分割**（同 4.2.4）で3つへ分ける。

⚠️ **加えて、既存サブ要求 `REQ-C02-S01` が Scope 段階と既習の突き合わせを担う。**
[`requirements-decisions/batch-07-options.md`](requirements-decisions/batch-07-options.md) 選択肢3に従い、
IDを保ったまま同じ階層へ置く。

| 局面 | 担当 |
|---|---|
| Scope 段階の受け取りと既習の突き合わせ | **`REQ-C02-S01`**（既存サブ要求） |
| 説明の提示 | `REQ-C02-R1`（新設） |
| 説明の深化 | `REQ-C02-R2`（新設） |
| 一次資料への復帰 | `REQ-C02-R3`（新設） |

---

### 5.2 既存サブ要求 `REQ-C02-S01` 既習の資料を横断で保持し突き合わせる

⚠️ **本節は既存サブ要求である**（第6バッチ選択肢3で確定）。

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは利用者が既に読んだ論文および学習した資料をプロジェクト横断で保持し、対象論文の前提知識と突き合わせて、既習の項目の説明を省けるようにするものとする。前提知識が不足したまま実装へ進もうとした場合に注意を出す。利用者の専門分野を設定として保持し、説明の例示と転用先の提案をその分野に寄せる。**熟達段階の推定は行わない**。 |

**＜Scope 段階の受け取り＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-S01.10` | 論文の取り込み後、Scope 段階が評価した「この論文を読むのに足りない前提知識」の一覧を受け取る。 |
| `REQ-C02-S01.20` | 受け取った一覧の各項目に、論文中の該当箇所への参照を持たせる。 |

**＜既習の保持と突き合わせ＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-S01.30` | 既に読んだ論文と学習した資料を、`DM-E18 LearningProfile` へプロジェクト横断で保持する。 |
| `REQ-C02-S01.40` | 保持するのは「読んだ」「受講した」という事実に限る。**理解度を保持しない。** |
| `REQ-C02-S01.50` | 既読の記録が、本製品で読んだ論文（内部）か、利用者の申告（外部）かを区別して保持する。 |
| `REQ-C02-S01.60` | Scope 段階の一覧と既習の記録を突き合わせ、既習の項目を識別する。 |
| `REQ-C02-S01.70` | 既習と識別した項目の説明を、既定では省略する。**利用者が求めれば表示する。** |

**＜不足時の注意＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-S01.80` | 前提知識が不足したまま実装の工程へ進もうとした場合、注意を表示する。 |
| `REQ-C02-S01.90` | 注意は**進行を妨げない。** 利用者が了解して進むことを許す。 |
| `REQ-C02-S01.100` | 了解して進んだ事実を記録する。後から不足を振り返れる状態にする。 |

**＜専門分野＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-S01.110` | 利用者の専門分野を設定として保持する。**自己申告のみとし、作業記録から推定しない。** |
| `REQ-C02-S01.120` | 説明の例示を、保持した専門分野に寄せる。 |
| `REQ-C02-S01.130` | 専門分野を設定していない状態でも、説明を提示できる。**設定を必須にしない。** |

> **`S01.40` と `S01.110` を分けて書いた理由。**
> どちらも「推定しない」ことを定めるが、対象が違う。前者は**理解度**、後者は**専門分野**である。
> 一方だけを禁じる実装が成立しうるため、条件ごとに行を分けた。
> 根拠は第6バッチ選択肢3の判断（`REQ-C04-S02`「流暢さだけで理解済みにしない」との衝突回避）。

---

### 5.3 下位要求 `REQ-C02-R1` 説明を示す

| 項目 | 内容 |
|---|---|
| **要求** | 利用者が語や数式を指定したとき、システムはその説明を**示し**、説明の水準を**明示し**、前提知識を利用の条件と**しない**ものとする。 |
| **理由** | 説明が無ければ読み進められず、説明があっても水準が分からなければ、自分に必要な深さか判断できない。前提を条件にすると、学びたい人ほど使えなくなる。 |
| **説明** | 説明の対象は用語・数式・機械学習の基礎の3種。実装単位は `F-16`（画面）と `B-04`（生成）。 |

**＜説明の対象＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R1.10` | 論文中の語を選択したとき、その語の説明を示す。 |
| `REQ-C02-R1.20` | 論文中の数式を選択したとき、記号の意味と式全体の意味を示す。 |
| `REQ-C02-R1.30` | 機械学習の基礎にあたる概念について、論文の記述に依存しない説明を示す。 |

**＜利用の条件＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R1.40` | 前提知識の有無を、機能を使うための条件としない。**未習の状態でも全機能を使える。** |
| `REQ-C02-R1.50` | 説明を読まずに次の工程へ進むことを妨げない。 |

**＜生成の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R1.60` | 説明を生成AIで作る場合、生成した旨と生成条件を記録する（`BR-02`）。 |
| `REQ-C02-R1.70` | 生成した説明と、一次資料から抽出した記述を区別して示す。 |
| `REQ-C02-R1.80` | 説明の内容を確定した事実として示さない。**利用者が原典で確かめられる状態にする**（`R3`）。 |

---

### 5.4 下位要求 `REQ-C02-R2` 説明を深める

| 項目 | 内容 |
|---|---|
| **要求** | 利用者が追加の説明を求めたとき、システムはより詳しい説明を**示し**、どこまで深めたかを**保持し**、元の水準へ**戻せるようにする**ものとする。 |
| **理由** | 一度の説明で足りるかは人によって違う。深めた結果しか残らないと、必要以上に詳しい説明を毎回読むことになる。 |
| **説明** | 深さの段階数は定めない。利用者が求めるかぎり深められる。 |

**＜深化の操作＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R2.10` | 表示中の説明に対し、追加の説明を求める操作を提供する。 |
| `REQ-C02-R2.20` | 追加の説明は、直前の説明を置き換えず、**両方を保持する。** |
| `REQ-C02-R2.30` | 深さの段階に上限を設けない。 |

**＜状態の保持＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R2.40` | どの語をどこまで深めたかを、プロジェクト内に保持する。 |
| `REQ-C02-R2.50` | 保持した深さを、次回の表示に反映する。 |
| `REQ-C02-R2.60` | 利用者が元の水準へ戻す操作を提供する。 |

⚠️ **`R2.40` の保持はプロジェクト内に限る。** プロジェクトを横断する保持は
`REQ-C02-S01` が扱い、**そちらは「読んだ」という事実だけを持つ。**
深さの段階は理解度に近い情報であるため、横断して保持しない。

---

### 5.5 下位要求 `REQ-C02-R3` 一次資料へ戻す

| 項目 | 内容 |
|---|---|
| **要求** | 説明を示すとき、システムはその根拠となる一次資料の箇所を**添え**、利用者がその箇所へ**移動でき**、移動先から元の説明へ**帰れるようにする**ものとする。 |
| **理由** | 説明を読んで納得した気になるだけでは理解に至らない。原典を自分で読んで初めて、説明が正しいかを判断できる。戻れなければ、確認のたびに読解の流れが切れる。 |
| **説明** | 一次資料は対象論文に限らない。参照した外部資料も含む。 |

**＜根拠の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R3.10` | 説明に、根拠となる一次資料の箇所への参照を添える。 |
| `REQ-C02-R3.20` | 根拠を特定できない説明は、**根拠なしと明示する。** 推測で参照を作らない。 |
| `REQ-C02-R3.30` | 根拠が対象論文の外にある場合、その資料の識別子と該当箇所を示す。 |

**＜移動＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C02-R3.40` | 参照から一次資料の該当箇所へ移動できる。 |
| `REQ-C02-R3.50` | 移動先から、元の説明へ戻れる。 |
| `REQ-C02-R3.60` | 移動しても、深めた説明の状態（`R2.40`）を失わない。 |

---

### 5.6 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| その語の説明を**示す** | `R1.10`〜`R1.80`、`S01.60`・`S01.70` |
| 求めに応じて説明を**深める** | `R2.10`〜`R2.60` |
| 一次資料の箇所へ**戻す** | `R3.10`〜`R3.60` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 前提用語と基礎説明を表示する | `R1.10`〜`R1.30` |
| 追加説明を要求できる | `R2.10`〜`R2.30` |
| 説明から一次資料の根拠へ戻れる | `R3.10`、`R3.40`、`R3.50` |
| 前提知識を利用条件としない | `R1.40`、`R1.50`、`S01.130` |

⚠️ **未確認事項。** Scope 段階が評価した一覧の形式は、`REQ-C03`（取り込み）の展開でも
`REQ-C02` の展開でも定めていない。**どちらが形式を定めるかを決める必要がある。**

---

## 6. REQ-C04 理解の確認とフィードバック【完全展開】

**読解経路の中核である。** `REQ-C02` が説明を提供するのに対し、本要求は**理解できたかを確かめる**。

### 6.1 上位要求

| カテゴリ | 学習支援（`LEARN`） |
|---|---|
| **要求 `REQ-C04`** | システムは主要な概念ごとに、理論の解説を**示し**、具体例と反例を**構成させ**、確認問題を**出し**、利用者の自己説明を**受け取り**、根拠を添えたフィードバックを**返す**ものとする。 |
| **理由** | 読んだだけでは理解の程度が分からない。自分の言葉で説明できて初めて理解といえるが、その判定を流暢さで行うと誤る。根拠を示して差を埋める。 |
| **説明** | 理解の判定は**利用者自身が行う。** システムは材料と根拠を提供し、判定を代行しない。実装単位は `F-16`（画面）と `B-04`（生成）。 |

**動詞の数を数える。** 「示す」「構成させる」「出す」「受け取る」「返す」の5つ。
8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「解説の提示」「演習と確認」「自己説明とフィードバック」に分かれるため、
**時系列分割**（同 4.2.4）で3つへ分ける。

⚠️ **うち2つは既存サブ要求が担う。**

| 局面 | 担当 |
|---|---|
| 理論の解説を示す | `REQ-C04-R1`（新設） |
| 具体例・反例・境界条件・手計算を構成させる | **`REQ-C04-S01`**（既存サブ要求） |
| 自己説明を受け取りフィードバックを返す | **`REQ-C04-S02`**（既存サブ要求） |

---

### 6.2 下位要求 `REQ-C04-R1` 主要概念ごとに理論の解説を示す

| 項目 | 内容 |
|---|---|
| **要求** | システムは論文の主要な概念を**識別し**、概念ごとに理論の解説を**示し**、解説と論文の記述の対応を**明示する**ものとする。 |
| **理由** | 論文は既知の概念を前提に書かれる。その前提を補わないと読み進められない。ただし補った説明が論文のどこに対応するかを示さないと、読解ではなく別の教材を読むことになる。 |
| **説明** | `REQ-C02`（前提知識の段階的説明）が語や数式の単位で説明するのに対し、本要求は**概念の単位**で理論を扱う。 |

**＜概念の識別＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-R1.10` | 論文から主要な概念を抽出し、一覧として示す。 |
| `REQ-C04-R1.20` | 抽出した各概念に、論文中の初出箇所への参照を持たせる。 |
| `REQ-C04-R1.30` | 利用者が概念を追加・削除できる。**抽出結果を確定として扱わない。** |

**＜解説の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-R1.40` | 概念ごとに、その理論的な位置づけと役割を示す。 |
| `REQ-C04-R1.50` | 解説と論文の記述の対応を明示する。論文が触れていない部分は、そう明示する。 |
| `REQ-C04-R1.60` | 解説を生成AIで作る場合、生成した旨と生成条件を記録する（`BR-02`）。 |

---

### 6.3 既存サブ要求 `REQ-C04-S01` 具体例と反例を構成させる

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは評価指標、理論的主張およびモデル挙動について、一次資料へ関連付けた具体例、反例、境界条件、手計算問題を提示し、利用者が入力データ、パラメータ、仮定または評価条件を変更して結果を再計算・比較できるようにする。フィードバックは定義、計算、観察、解釈、適用範囲を区別し、**複数の妥当な例または解法を許容する**ものとする。 |

**＜課題の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S01.10` | 評価指標・理論的主張・モデル挙動のそれぞれについて、具体例を提示する。 |
| `REQ-C04-S01.20` | 仮定が成立しない反例を提示する。**具体例だけを示さない。** |
| `REQ-C04-S01.30` | 主張が成立する境界の条件を提示する。 |
| `REQ-C04-S01.40` | 手で計算できる規模の問題を提示する。 |
| `REQ-C04-S01.50` | 各課題を、対象の概念・数式・主張・前提のいずれかへ関連付ける。 |
| `REQ-C04-S01.60` | 課題を、問題・入力条件・確認点・期待する観察・解答例・根拠の6つに区別して示す。 |

**＜条件の変更と再計算＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S01.70` | 利用者が入力データ・パラメータ・仮定・評価条件を変更できる。 |
| `REQ-C04-S01.80` | 変更後の結果を再計算し、変更前と並べて比較できる。 |
| `REQ-C04-S01.90` | 変更した条件と結果の対を記録する。 |

**＜フィードバックの区別＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S01.100` | フィードバックを、定義・計算・観察・解釈・適用範囲の5つに区別して返す。 |
| `REQ-C04-S01.110` | **複数の妥当な例または解法を許容する。** 解答例と一致しないことだけを理由に誤答としない。 |
| `REQ-C04-S01.120` | 文字列の一致だけで正誤を判定しない。 |
| `REQ-C04-S01.130` | 回答と訂正の履歴を保存する。 |

**＜外部モデルの観察の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S01.140` | 少数の外部モデルの観察だけで、一般的な性質を確定しない。 |
| `REQ-C04-S01.150` | 観察の件数と条件を、結論とともに記録する。 |

---

### 6.4 既存サブ要求 `REQ-C04-S02` 自己説明を受け取る

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは利用者が理解した内容を自分の言葉で説明し、理解できている点、説明できない点、未解決の疑問および次に深掘りする項目を記録できるようにするものとする。 |
| **受入基準**（同上） | 自己説明を根拠箇所へ関連付け、理解済み・部分理解・未理解・要再確認を区別し、フィードバックと訂正履歴を保存し、**流暢さだけで理解済みにしない**。 |

**＜自己説明の記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S02.10` | 利用者が自分の言葉で説明した内容を受け取り、記録する。 |
| `REQ-C04-S02.20` | 自己説明を、論文中の根拠箇所へ関連付ける。 |
| `REQ-C04-S02.30` | 利用者が、理解できている点・説明できない点・未解決の疑問・次に深掘りする項目を分けて記録できる。 |

**＜理解の状態＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S02.40` | 理解の状態を、理解済み・部分理解・未理解・要再確認の4つに区別する。 |
| `REQ-C04-S02.50` | **状態を確定するのは利用者である。** システムが自動で理解済みへ変更しない。 |
| `REQ-C04-S02.60` | 状態を後から変更できる。変更の履歴を保存する。 |

**＜⚠️ 流暢さによる判定の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S02.70` | 自己説明の**長さ・語彙の豊富さ・文法の正しさを、理解の判定の根拠としない。** |
| `REQ-C04-S02.80` | 自己説明に対するフィードバックは、**根拠箇所との対応**を示す。説明の巧拙を評価しない。 |
| `REQ-C04-S02.90` | 説明に含まれていない要素を指摘する場合、その要素が論文のどこにあるかを示す。 |
| `REQ-C04-S02.100` | LLM の出力だけで理解済みの状態へ変更しない。 |

> **`S02.70` を独立させた理由。**
> 受入基準の「流暢さだけで理解済みにしない」を1行で書くと、**何を流暢さとみなすかが定まらない。**
> 長さ・語彙・文法の3つを明示することで、実装が判定に使ってよい情報とそうでない情報が分かれる。
> 根拠は [`requirements-decisions/batch-06-options.md`](requirements-decisions/batch-06-options.md) 1.3節
> （熟達段階の推定が本受入基準と衝突しうるという分析）。

**＜フィードバックの保存＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C04-S02.110` | フィードバックと、利用者による訂正の履歴を保存する。 |
| `REQ-C04-S02.120` | 自己説明を `DM-E15 SelfExplanation` へ保存する。プロジェクトに紐づける。 |

⚠️ **自己説明はプロジェクトを横断して保持しない。**
横断で保持するのは「読んだ／受講した」という事実に限る（`REQ-C02-S01`、第6バッチ選択肢3）。
自己説明は理解度に近い情報であるため、`DM-E18 LearningProfile` へ移さない。

---

### 6.5 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 理論の解説を**示す** | `R1.10`〜`R1.60` |
| 具体例と反例を**構成させる** | `S01.10`〜`S01.90` |
| 確認問題を**出す** | `S01.40`、`S01.60` |
| 利用者の自己説明を**受け取る** | `S02.10`〜`S02.60` |
| 根拠を添えたフィードバックを**返す** | `S01.100`〜`S01.150`、`S02.70`〜`S02.110` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 主要概念ごとに理解確認を提示する | `R1.10`、`S01.10`〜`S01.60` |
| 利用者の回答へ根拠付きフィードバックを返せる | `S01.100`、`S02.80`、`S02.90` |
| 流暢さだけで理解済みにしない | `S02.70`、`S02.100` |
| 複数解法を文字列一致だけで誤答にしない | `S01.110`、`S01.120` |

⚠️ **`REQ-C02` との境界。** `REQ-C02` は語や数式の単位で説明し、本要求は概念の単位で理論を扱う。
**同じ対象に両方が説明を出す場合がある。** 重複を許すか、どちらかへ寄せるかは未定である。

---

## 7. REQ-C06 実行言語の段階拡張と承認ゲート【完全展開】

**`REQ-C05` が「隔離した環境で実行する」と定めるのに対し、本要求はその隔離そのものを定める。**

### 7.1 上位要求

| カテゴリ | 再現実装（`REPRO`） |
|---|---|
| **要求 `REQ-C06`** | 実行を要求されたとき、システムは対象の言語に応じたサンドボックスを**選び**、危険な操作を**検出し**、人間の承認を**求め**、承認を得てから**実行する**ものとする。 |
| **理由** | 承認を迂回できる経路が1つでもあると、迂回できない保証が崩れる。言語を増やすたびに隔離の条件も変わるため、言語ごとに用意する。 |
| **説明** | 初期実行対象は Python。C/C++ 等は言語アダプタと専用サンドボックスで段階的に追加する。実装単位は `B-03`（言語アダプタ）、`B-05`（サンドボックス）、`F-12`（承認ゲート）。 |

**動詞の数を数える。** 「選ぶ」「検出する」「求める」「実行する」の4つ。
8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「サンドボックスの選択」「危険な操作の検出」「承認と実行」に分かれるため、
**時系列分割**（同 4.2.4）で3つへ分ける。

⚠️ **本要求に既存サブ要求は無い。** すべて新設の下位要求で構成する。

### 7.1.1 `REQ-C05` との境界

| 定めるもの | 担当 |
|---|---|
| ホスト環境で直接実行しない | `REQ-C05-R2.10`（**隔離を使う側の前提**） |
| シークレットが生成物へ含まれていないことの検査 | `REQ-C05-R2.20` |
| 初期リリースは CPU のみ・ネットワーク遮断 | `REQ-C05-R2.30` |
| **サンドボックスの構成と言語ごとの差** | **本章 `R1`** |
| **危険な操作の検出** | **本章 `R2`** |
| **承認ゲートの成立条件** | **本章 `R3`** |

**`REQ-C05` は隔離を前提として使い、本章は隔離を作る。** 重複しない。

---

### 7.2 下位要求 `REQ-C06-R1` 言語に応じたサンドボックスを選ぶ

| 項目 | 内容 |
|---|---|
| **要求** | システムは実行の対象となるコードの言語を**判別し**、その言語に対応するサンドボックスを**選び**、対応するものが無い場合は実行せずに**その旨を示す**ものとする。 |
| **理由** | 言語ごとに実行の仕組みも危険な操作も違う。ひとつのサンドボックスで全言語を扱うと、後から言語を足すたびに既存の隔離条件を緩めることになる。 |
| **説明** | 初期リリースは Python のみ。言語アダプタ方式（`B-03`）で追加する。**アダプタが無い言語のコードは実行しない。** |

**＜言語の判別＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R1.10` | 実行対象のコードの言語を判別する。 |
| `REQ-C06-R1.20` | 判別できなかった場合、実行しない。 |
| `REQ-C06-R1.30` | 判別の結果を実行の記録へ残す。 |

**＜サンドボックスの選択＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R1.40` | 判別した言語に対応する専用のサンドボックスを選ぶ。 |
| `REQ-C06-R1.50` | 対応するサンドボックスが無い言語の場合、**実行せず**、未対応である旨を示す。 |
| `REQ-C06-R1.60` | 汎用のサンドボックスで代替しない。 |
| `REQ-C06-R1.70` | 初期リリースで用意するサンドボックスは Python 用のみとする。 |

**＜言語の追加＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R1.80` | 言語アダプタは共通のインタフェースを実装する。中核の実行処理は個々の言語を知らない。 |
| `REQ-C06-R1.90` | 言語を追加するとき、その言語専用の隔離条件を定義する。 |
| `REQ-C06-R1.100` | 言語の追加によって、既存の言語の隔離条件を緩めない。 |
| `REQ-C06-R1.110` | 言語ごとの隔離条件を審査したうえで追加する。審査の記録を残す。 |

> **`R1.60` を独立させた理由。**
> 「対応するサンドボックスが無ければ実行しない」だけでは、**汎用のサンドボックスを用意して
> それで代替する実装が成立してしまう。** それは言語ごとに隔離条件を変える趣旨を損なう。
> 禁止事項として明示した。

---

### 7.3 下位要求 `REQ-C06-R2` 危険な操作を検出する

| 項目 | 内容 |
|---|---|
| **要求** | システムは実行前にコードを**検査し**、危険な操作を**検出し**、検出した内容を利用者へ**示す**ものとする。 |
| **理由** | 第三者のコードをそのまま実行すると、意図しない動作を起こしうる。実行してから気づいても遅い。何が危険とみなされたかを示さなければ、利用者は承認の判断ができない。 |
| **説明** | 検出は実行を止めるためではなく、**人間が承認を判断するための材料**である。検出されても利用者が承認すれば実行する。 |

**＜検査の実施＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R2.10` | 実行前にコードを静的に検査する。 |
| `REQ-C06-R2.20` | 検査の対象を、公式実装から取得したコードと、生成したコードの両方とする。 |
| `REQ-C06-R2.30` | 検査を言語ごとの基準で行う。共通の基準だけに頼らない。 |

**＜検出の対象＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R2.40` | 外部への通信を伴う操作を検出する。 |
| `REQ-C06-R2.50` | サンドボックス外のファイルへの書き込みを検出する。 |
| `REQ-C06-R2.60` | 環境変数や資格情報の読み取りを検出する。 |
| `REQ-C06-R2.70` | 実行時にコードを生成して評価する操作を検出する。 |

**＜検出結果の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R2.80` | 検出した操作を、コード中の該当箇所とともに示す。 |
| `REQ-C06-R2.90` | 検出しなかったことを「安全である」と示さない。**検査の限界を明示する。** |
| `REQ-C06-R2.100` | 検出の結果を実行の記録へ残す。 |

⚠️ **`R2.90` は重要である。** 静的検査で検出できない操作は必ず存在する。
「検出なし」を安全の保証として示すと、利用者が確認を省く。

---

### 7.4 下位要求 `REQ-C06-R3` 承認を求めてから実行する

| 項目 | 内容 |
|---|---|
| **要求** | システムは実行の前に人間の承認を**求め**、承認を得るまで**実行せず**、承認と実行の対応を**記録する**ものとする。 |
| **理由** | 承認を迂回できる経路が1つでもあると、迂回できない保証が崩れる。承認したことと実行したことの対応が残らなければ、後から何を承認したのか分からない。 |
| **説明** | 承認の対象は、コード実行・外部通信・重要な解釈・重大な証拠矛盾・Phase 末の5つ（`F-12`）。**本章はそのうちコード実行を扱う。** |

**＜承認の要求＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R3.10` | コードを実行する前に、人間の承認を求める。 |
| `REQ-C06-R3.20` | 承認を求めるとき、実行対象・選んだサンドボックス・検出した危険な操作を示す。 |
| `REQ-C06-R3.30` | 高リスクと判定した操作を含む場合、その旨を明示して承認を求める。 |

**＜⚠️ 迂回の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R3.40` | 承認を得ずにコードを実行する経路を、**画面にも API にも設けない。** |
| `REQ-C06-R3.50` | 承認を省略する設定を設けない。 |
| `REQ-C06-R3.60` | 一度の承認で以後の実行をすべて許可する仕組みを設けない。**実行ごとに承認を求める。** |
| `REQ-C06-R3.70` | LLM が承認を代行しない。承認は人間の明示的な操作に限る。 |

> **`R3.40`〜`R3.70` を4つに分けた理由。**
> 「迂回不可能な承認ゲート」を1行で書くと、**どの迂回経路を禁じるのかが定まらない。**
> 画面・API・設定・一括承認・代行の5つは、いずれも「承認を経ずに実行できる」状態を作りうる。
> 受入基準が「迂回不可能」と定める以上、**経路ごとに禁止を明示する。**

**＜Phase 末の承認＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R3.80` | 各 Phase の末尾で、次の Phase へ進む承認を求める。 |
| `REQ-C06-R3.90` | Phase を飛ばして進む操作を設けない。 |

**＜記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C06-R3.100` | 承認の日時・対象・承認者・承認時に示した内容を記録する。 |
| `REQ-C06-R3.110` | 承認と、それにもとづく実行の対応を記録する。 |
| `REQ-C06-R3.120` | 承認の記録を `DM-E13 Approval` へ保存する。**訂正は新しい履歴として残し、上書きしない。** |

---

### 7.5 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 言語に応じたサンドボックスを**選ぶ** | `R1.10`〜`R1.110` |
| 危険な操作を**検出する** | `R2.10`〜`R2.100` |
| 人間の承認を**求める** | `R3.10`〜`R3.30`、`R3.80`、`R3.90` |
| 承認を得てから**実行する** | `R3.40`〜`R3.70`、`R3.100`〜`R3.120` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 言語追加用インターフェース | `R1.80` |
| 言語別検証 | `R2.30`、`R1.110` |
| 専用隔離条件 | `R1.40`、`R1.60`、`R1.90`、`R1.100` |
| 迂回不可能な承認ゲート | `R3.40`〜`R3.70`、`R3.90` |

### 7.6 `REQ-C05` との境界の検査結果

`REQ-C05` の展開時に「隔離の条件と `REQ-C05-R2.10`〜`R2.30` の境界を検査する」と残していた。

**重複は無い。**

| `REQ-C05-R2` の仕様 | 性質 | 本章との関係 |
|---|---|---|
| `R2.10` ホスト直接実行の禁止 | **隔離を使う側の前提** | 本章 `R1.40`〜`R1.70` が隔離そのものを定める |
| `R2.20` シークレットの検査 | 実行前の検査だが、対象は**生成物** | 本章 `R2.60` は**コード中の読み取り操作**を検出する。対象が違う |
| `R2.30` CPU のみ・ネットワーク遮断 | 初期リリースの**範囲の宣言** | 本章 `R1.90` が言語ごとの隔離条件を定義する |

⚠️ **`REQ-C05-R2.20` と本章 `R2.60` は近いが別である。**
前者は「生成物にシークレットが混入していないか」、後者は「コードがシークレットを読もうとしていないか」。
**混入の検査と、読み取りの検出**という違いがある。

---

## 8. REQ-C07 証拠の記録と矛盾の保持【完全展開】

**何を根拠にそう言えるのかを残す要求である。** `REQ-C08`（由来の版管理）は本要求が記録した証拠を
原文から成果物まで繋ぐため、**本要求が先に決まる必要がある。**

### 8.1 上位要求

| カテゴリ | 批判的検証（`VERIFY`） |
|---|---|
| **要求 `REQ-C07`** | システムは主張ごとに情報源を**集め**、属性と一致状況を**記録し**、食い違いを**そのまま並べて示し**、人間が採用した根拠を**受け取って保存する**ものとする。 |
| **理由** | 矛盾を自動で解消すると、解消の妥当性を後から検証できない。どちらを採ったかではなく、なぜ採ったかが残らなければ意味がない。 |
| **説明** | 証拠は `DM-E07 Evidence`、実験条件は `DM-E08 ExperimentCond` へ保存する。実装単位は `B-02`（収集）、`B-12`（証拠台帳）、`F-17`（表示）、`F-05`／`F-06`（編集）。 |

**動詞の数を数える。** 「集める」「記録する」「示す」「受け取って保存する」の4つ。
8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「収集と記録」「矛盾の提示」「採用の承認」に分かれるため、
**時系列分割**（同 4.2.4）で3つへ分ける。

⚠️ **加えて、既存サブ要求 `REQ-C07-S01` が信頼性の参考情報の扱いを担う。**

| 局面 | 担当 |
|---|---|
| 情報源の収集と属性の記録 | `REQ-C07-R1`（新設） |
| 信頼性の参考情報の表示 | **`REQ-C07-S01`**（既存サブ要求） |
| 矛盾の保持と提示 | `REQ-C07-R2`（新設） |
| 採用根拠の承認と保存 | `REQ-C07-R3`（新設） |

---

### 8.2 下位要求 `REQ-C07-R1` 情報源を集め属性を記録する

| 項目 | 内容 |
|---|---|
| **要求** | システムは主張ごとに情報源を**集め**、その属性を**記録し**、主張との一致状況を**分類する**ものとする。 |
| **理由** | どこから得た情報かが残らないと、後から確かめられない。一致しているのか食い違っているのかを分類しないと、矛盾の存在自体に気づけない。 |
| **説明** | 情報源は公式実装・査読コメント・採択情報・引用・著者実績・専門家意見・重要文献・データセット・二次解説を含む（`B-02`）。 |

**＜情報源の収集＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R1.10` | 主張を単位として情報源を集める。文書全体ではなく主張ごとに紐づける。 |
| `REQ-C07-R1.20` | 情報源の種別を、公式実装・査読・採択・引用・著者実績・専門家意見・重要文献・データセット・二次解説へ区別する。 |
| `REQ-C07-R1.30` | 収集の時点で真偽を判定しない。**収集と判定を分ける。** |

**＜属性の記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R1.40` | 情報源ごとに、種別・主体・情報源の所在・取得日時・対象版を記録する。 |
| `REQ-C07-R1.50` | 情報源の直接性を記録する。主張を直接支持するのか、推論を挟むのかを区別する。 |
| `REQ-C07-R1.60` | 取得できなかった属性を `unknown` として記録する。推測で補わない。 |

**＜一致状況の分類＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R1.70` | 情報源と主張の関係を、支持・反証・部分的支持・無関係・判定不能へ分類する。 |
| `REQ-C07-R1.80` | 分類を確定できない場合、判定不能として記録する。**推測で他の分類を割り当てない。** |
| `REQ-C07-R1.90` | 確信度を記録する。確信度は情報源ごとに持ち、主張全体の確信度として合算しない。 |

> **`R1.90` の後段を書いた理由。**
> 確信度を合算すると、**弱い証拠が多数あるだけで確信度が高くなる。**
> `REQ-C07-S01` が「一つの指標で真偽を自動確定しない」と定める趣旨に反するため、合算を禁じた。

---

### 8.3 既存サブ要求 `REQ-C07-S01` 信頼性の参考情報を種類別に示す

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは査読・採択、引用、著者実績、専門家意見、ウェブ上の議論を信頼性の参考情報として種類別に表示し、**一つの指標で真偽を自動確定しない**ものとする。 |
| **受入基準**（同上） | 参考情報の種類、主体、情報源、取得日時、対象版を表示し、矛盾を併記し、利用者の判断理由を保存できる。 |

**＜参考情報の表示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-S01.10` | 査読・採択、引用、著者実績、専門家意見、ウェブ上の議論を、種類ごとに分けて表示する。 |
| `REQ-C07-S01.20` | 各参考情報に、種類・主体・情報源の所在・取得日時・対象版を添える。 |
| `REQ-C07-S01.30` | 参考情報を、主張そのものの証拠（`R1`）と区別して表示する。 |

**＜⚠️ 自動確定の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-S01.40` | 査読の有無だけで、主張の真偽を確定しない。 |
| `REQ-C07-S01.50` | 引用数だけで、主張の重要性や正しさを確定しない。 |
| `REQ-C07-S01.60` | 著者の実績だけで、主張の真偽を確定しない。 |
| `REQ-C07-S01.70` | 複数の参考情報を数値化して合成し、単一の信頼度として示さない。 |

> **`S01.40`〜`S01.70` を4つに分けた理由。**
> 「一つの指標で真偽を自動確定しない」を1行で書くと、**どの指標を指すのかが定まらない。**
> 査読・引用・著者実績の3つは、いずれも単独で判定に使われやすい。
> `S01.70` は、**それらを合成すれば単一の指標ではないと解釈する余地**を塞ぐために足した。

**＜判断理由の保存＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-S01.80` | 利用者が参考情報をどう解釈したかを記録できる。 |
| `REQ-C07-S01.90` | 参考情報どうしが食い違う場合、併記する。**片方を選んで表示しない。** |

---

### 8.4 下位要求 `REQ-C07-R2` 矛盾をそのまま並べて示す

| 項目 | 内容 |
|---|---|
| **要求** | 情報源どうしが食い違うとき、システムは双方を**保持し**、未解決であることを**示し**、自動で**解消しない**ものとする。 |
| **理由** | 矛盾を自動で解消すると、解消の妥当性を後から検証できない。片方を消すと、なぜその判断に至ったかを追えなくなる。 |
| **説明** | 矛盾の検出は自動で行ってよい。**禁じるのは解消である。** |

**＜矛盾の検出＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R2.10` | 同じ主張に対する情報源の一致状況が食い違う場合、矛盾として検出する。 |
| `REQ-C07-R2.20` | 検出した矛盾に、どの情報源とどの情報源が食い違うかを記録する。 |

**＜⚠️ 自動解消の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R2.30` | 矛盾する情報源のどちらかを、システムが削除しない。 |
| `REQ-C07-R2.40` | 矛盾する情報源のどちらかを、システムが非表示にしない。 |
| `REQ-C07-R2.50` | 情報源の種別や新しさだけを理由に、一方を優先しない。 |
| `REQ-C07-R2.60` | LLM の判断だけで矛盾を解決済みにしない。 |

**＜未解決の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R2.70` | 未解決の矛盾を、解決済みのものと区別して表示する。 |
| `REQ-C07-R2.80` | 重大な矛盾を、利用者が見落としにくい形で示す。 |
| `REQ-C07-R2.90` | 未解決の矛盾がある状態でも、後続の工程へ進めるようにする。**進行を止めない。** |
| `REQ-C07-R2.100` | 未解決のまま進んだ場合、その事実を記録する。 |

⚠️ **`R2.90` と `R2.100` は対である。** 矛盾があるだけで作業が止まると、
利用者は矛盾を無視する方法を探す。進めることを許したうえで、進んだ事実を残す。

---

### 8.5 下位要求 `REQ-C07-R3` 採用の根拠を受け取って保存する

| 項目 | 内容 |
|---|---|
| **要求** | システムは矛盾に対して利用者がどちらを採用したかを**受け取り**、採用の理由を**受け取り**、両方を**保存する**ものとする。 |
| **理由** | どちらを採ったかだけでは、後から妥当性を検証できない。なぜ採ったかが残って初めて、判断を見直せる。 |
| **説明** | 採用の記録は `DM-E13 Approval` と `DM-E07 Evidence` の両方に関わる。承認の履歴は上書きせず追記する（`BR-11`）。 |

**＜採用の受け取り＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R3.10` | 矛盾する情報源のうち、どれを採用するかを利用者が選べる。 |
| `REQ-C07-R3.20` | 採用の理由を利用者が入力できる。**理由を空のまま採用を確定できない。** |
| `REQ-C07-R3.30` | どれも採用せず、未解決のまま保留する選択を許す。 |

**＜保存＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R3.40` | 採用した情報源・採用しなかった情報源・採用の理由・日時を保存する。 |
| `REQ-C07-R3.50` | 採用しなかった情報源を削除しない。 |
| `REQ-C07-R3.60` | 採用の判断を後から変更できる。変更の履歴を追記として残し、**上書きしない。** |

**＜対象実験との整合＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C07-R3.70` | 採用の判断が特定の実験条件に依存する場合、その条件を記録する。 |
| `REQ-C07-R3.80` | 条件が変われば判断も変わりうることを、記録の上で区別できる状態にする。 |

---

### 8.6 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 主張ごとに情報源を**集める** | `R1.10`〜`R1.30` |
| 属性と一致状況を**記録する** | `R1.40`〜`R1.90`、`S01.10`〜`S01.30` |
| 食い違いをそのまま並べて**示す** | `R2.10`〜`R2.100`、`S01.90` |
| 人間が採用した根拠を**受け取って保存する** | `R3.10`〜`R3.80`、`S01.80` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 複数情報源を区別して併記する | `R1.20`、`S01.10`、`S01.90` |
| 矛盾を未解決表示する | `R2.70`、`R2.80` |
| 人間の採用根拠を保存できる | `R3.20`、`R3.40` |
| 一つの指標で真偽を自動確定しない | `S01.40`〜`S01.70`、`R1.90` |

⚠️ **`REQ-C10` との境界。** `REQ-C10`（批判的検証ワークスペース）も主張と証拠を扱い、
実装単位 `B-12` を共有する。**本要求は情報源の記録と矛盾の保持を、`REQ-C10` は主張の区分と
成立範囲の検討を担う**という切り分けだが、`REQ-C10` の展開時に重複を検査する。

---

## 9. REQ-C08 由来の版管理と双方向追跡【完全展開】

**`REQ-C07` が記録した証拠を、原文から成果物まで繋ぐ要求である。**
何を記録するかが決まって初めて、その由来を設計できる。

### 9.1 上位要求

| カテゴリ | 批判的検証（`VERIFY`） |
|---|---|
| **要求 `REQ-C08`** | システムは原文から成果物までの由来を、生成条件・変換・レビュー・支持反証関係とともに**版管理し**、根拠から成果物へも成果物から根拠へも**追跡できるようにする**ものとする。 |
| **理由** | 成果物だけが残っても、それが何にもとづくのか分からなければ検証できない。片方向にしか辿れないと、根拠が誤っていたときにどの成果物へ影響するかを調べられない。 |
| **説明** | **段階1→4で拡張する要求である。** 初期リリースは段階1に相当する範囲のみを実装する。実装単位は `B-13`（プロヴェナンス管理）、`F-17`（表示）、`N-06`（監査ログ）、`N-09`（根拠履歴の完全性）。 |

**動詞の数を数える。** 「版管理する」「追跡できるようにする」の2つ。
8個未満なので**分割しないでよい**（REF-17 4.2.2）。

ただし振る舞いの局面が「由来の記録」「版管理」「双方向の追跡」「段階の移行」に分かれるため、
**時系列分割**（同 4.2.4）で4つへ分ける。

⚠️ **既存サブ要求 `REQ-C08-S01` は未具体である。** 9.6節に枠だけ置く。

| 局面 | 担当 |
|---|---|
| 由来の記録 | `REQ-C08-R1`（新設） |
| 版管理 | `REQ-C08-R2`（新設） |
| 双方向の追跡 | `REQ-C08-R3`（新設） |
| 段階の移行 | `REQ-C08-R4`（新設） |
| 未具体 | **`REQ-C08-S01`** |

### 9.1.1 ⚠️ 段階の定義と本章の範囲

受入基準は「1→4の各段階を検証でき、前段階のデータを移行し、根拠と成果物を双方向追跡できる」
と定める。**段階の存在自体が要求の一部である。**

| 段階 | 記録する範囲 | 本章での扱い |
|---|---|---|
| **段階1** | 原文・要約・解釈・生成条件 | ✅ **仕様まで展開する** |
| 段階2 | 分類・問題・条件変更・予想 | 枠のみ |
| 段階3 | 計算・観察・反例・仮説・利用者訂正 | 枠のみ |
| 段階4 | 要求・実装・テスト・承認・支持反証関係 | 枠のみ |

**本章が仕様まで落とすのは段階1に限る。** 段階2〜4は、何が増えるかを枠として示すが、
**中身が未確定であるため仕様化しない。** 推測で仕様を書くことは `BR-10` の趣旨に反する。

⚠️ **ただし段階の移行（`R4`）は段階1の時点で仕様化する。**
`N-09` が「段階拡張時に後方互換な移行を行う」と定めており、
**移行できる形で段階1を作らなければ、後から作り直すことになる。**

---

### 9.2 下位要求 `REQ-C08-R1` 由来を記録する

| 項目 | 内容 |
|---|---|
| **要求** | システムは成果物を生成したとき、その入力・生成条件・変換の内容を**記録し**、入力と成果物の関係を**保存する**ものとする。 |
| **理由** | 生成条件が残らなければ、同じ結果を再び得られるかを確かめられない。何を入力に何が出たかの対が残らなければ、辿ることもできない。 |
| **説明** | 段階1が記録するのは、原文・要約・解釈・生成条件の4種である。 |

**＜記録する対象（段階1）＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R1.10` | 取り込んだ原文を、由来の起点として記録する。 |
| `REQ-C08-R1.20` | 原文から生成した要約を、原文への参照とともに記録する。 |
| `REQ-C08-R1.30` | 原文または要約から生成した解釈を、その入力への参照とともに記録する。 |

**＜生成条件＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R1.40` | 生成AIを用いた場合、モデル名・日時を記録する（`BR-02`）。 |
| `REQ-C08-R1.50` | 生成に用いた入力の範囲を記録する。原文全体か特定の箇所かを区別する。 |
| `REQ-C08-R1.60` | 人間が作成したものと、生成AIが作成したものを区別して記録する。 |
| `REQ-C08-R1.70` | 生成条件を記録できなかった場合、`unknown` として記録する。**推測で補わない。** |

**＜変換の記録＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R1.80` | 入力から成果物への変換を、1件ずつ独立した記録として保存する。 |
| `REQ-C08-R1.90` | 1つの成果物が複数の入力を持つ場合、すべての入力を記録する。 |
| `REQ-C08-R1.100` | 記録を `DM-E12 Provenance` へ保存する。 |

---

### 9.3 下位要求 `REQ-C08-R2` 由来を版管理する

| 項目 | 内容 |
|---|---|
| **要求** | システムは由来の記録を変更するとき、既存の記録を**上書きせず**、新しい版として**追記し**、版どうしの関係を**保存する**ものとする。 |
| **理由** | 上書きすると、以前どう判断したかが失われる。判断が変わった経緯こそが、後から検証するときの手がかりになる。 |
| **説明** | `BR-11`（保存・削除・共有）が定める「上書きしない」を、由来の記録へ適用する。 |

**＜版の追記＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R2.10` | 由来の記録を変更するとき、既存の記録を**上書きしない。** |
| `REQ-C08-R2.20` | 変更を新しい版として追記する。 |
| `REQ-C08-R2.30` | 新しい版に、直前の版への参照を持たせる。 |
| `REQ-C08-R2.40` | 版ごとに、作成の日時と作成の主体を記録する。 |

**＜識別と完全性＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R2.50` | 各版に一意の識別子を持たせる。 |
| `REQ-C08-R2.60` | 記録の内容に対するハッシュを保持し、後から改変を検出できるようにする（`N-09`）。 |
| `REQ-C08-R2.70` | 承認の履歴を、由来の記録と関連付けて保持する。 |

**＜削除の扱い＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R2.80` | 由来の記録を削除する操作を、**利用者の明示的な確認なしに実行しない。** |
| `REQ-C08-R2.90` | 削除した場合も、削除した事実と日時を記録に残す。 |

---

### 9.4 下位要求 `REQ-C08-R3` 双方向に追跡する

| 項目 | 内容 |
|---|---|
| **要求** | システムは利用者が根拠を指定したとき、そこから導かれた成果物を**示し**、成果物を指定したとき、その根拠を**示す**ものとする。 |
| **理由** | 根拠が誤っていたと分かったとき、どの成果物へ影響するかを調べられなければ、訂正の範囲が決まらない。逆に成果物の妥当性を疑ったとき、根拠へ遡れなければ確かめられない。 |
| **説明** | 表示は `F-17`（証拠・プロヴェナンス表示）が担う。 |

**＜根拠から成果物へ＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R3.10` | 根拠を指定したとき、そこから直接導かれた成果物を示す。 |
| `REQ-C08-R3.20` | 間接的に導かれた成果物も、経路とともに示す。 |
| `REQ-C08-R3.30` | 影響の範囲を、直接と間接に区別して示す。 |

**＜成果物から根拠へ＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R3.40` | 成果物を指定したとき、その直接の入力を示す。 |
| `REQ-C08-R3.50` | 原文まで遡れる経路を示す。 |
| `REQ-C08-R3.60` | 経路の途中に `unknown` がある場合、そこで途切れることを示す。**推測で繋がない。** |

**＜訂正の伝播＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R3.70` | 根拠を訂正したとき、影響する成果物の一覧を示す。 |
| `REQ-C08-R3.80` | 影響する成果物を、**システムが自動で書き換えない。** 利用者が判断する。 |
| `REQ-C08-R3.90` | 訂正の影響を確認していない成果物を、そう分かる形で示す。 |

> **`R3.80` を独立させた理由。**
> 「双方向に追跡できる」だけでは、**追跡した先を自動で更新する実装が成立してしまう。**
> 根拠が変われば成果物も変わるとは限らず、判断は人間が行う（`REQ-C10`）。
> 追跡と更新を分けるため、禁止を明示した。

---

### 9.5 下位要求 `REQ-C08-R4` 段階を移行する

| 項目 | 内容 |
|---|---|
| **要求** | システムは記録の範囲を次の段階へ広げるとき、前段階のデータを**保持したまま**、新しい項目を**追加し**、前段階の記録を**読める状態に保つ**ものとする。 |
| **理由** | 段階を進めるたびに過去の記録が読めなくなると、蓄積の意味がない。後方互換でない移行は、事実上のやり直しになる。 |
| **説明** | `N-09` が「段階拡張時に後方互換な移行を行う」と定める。**段階1の時点でこの仕様を満たす形にする。** |

**＜移行の前提＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R4.10` | 記録に、どの段階で作られたかを示す情報を持たせる。 |
| `REQ-C08-R4.20` | 段階ごとに増える項目を、既存の項目と独立して追加できる構造にする。 |
| `REQ-C08-R4.30` | 前段階の記録に無い項目を、`unknown` ではなく**その段階には存在しなかった**として区別する。 |

**＜移行の実施＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R4.40` | 段階を進めるとき、前段階のデータを削除しない。 |
| `REQ-C08-R4.50` | 移行の前後で、識別子とハッシュを保持する。 |
| `REQ-C08-R4.60` | 移行を実施した事実と日時を記録する。 |

**＜段階の検証＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C08-R4.70` | 現在どの段階まで記録しているかを示す。 |
| `REQ-C08-R4.80` | 段階ごとに、その段階が求める項目が揃っているかを検証できる。 |

⚠️ **`R4.30` は後から効く。** 段階2で追加される「分類」の項目は、段階1の記録には存在しない。
これを `unknown`（取得できなかった）と扱うと、**再取得すれば埋まるかのように見える。**
区別しないと、後から誤った補完を招く。

---

### 9.6 既存サブ要求 `REQ-C08-S01`（未具体）

⚠️ **本サブ要求は要求文が未具体である。**
[`requirements.md`](requirements.md) 第3.3節が「後続小節の詳細分析で具体化する」としている。

**本章では枠のみを置く。** 具体化されたときに、他のサブ要求と同じ形で仕様を展開する
（[`requirements-decisions/batch-07-options.md`](requirements-decisions/batch-07-options.md) 選択肢3）。

| 項目 | 内容 |
|---|---|
| **要求** | 未具体。 |
| **状態** | 対応する一次資料の小節が未分析。 |

---

### 9.7 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 由来を**版管理する** | `R1.10`〜`R1.100`、`R2.10`〜`R2.90` |
| 双方向に**追跡できるようにする** | `R3.10`〜`R3.90` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 1→4の各段階を検証できる | `R4.70`、`R4.80` |
| 前段階のデータを移行できる | `R4.10`〜`R4.60` |
| 根拠と成果物を双方向追跡できる | `R3.10`〜`R3.60` |

⚠️ **段階2〜4は仕様化していない。** 9.1.1節のとおり、中身が未確定であるため。
**段階を進めるときに、その段階の仕様を展開する。**

⚠️ **`REQ-C10` との境界。** 実装単位 `B-13` を共有する。
本要求は**由来の記録と追跡**を、`REQ-C10` は**主張の検証**を担うという切り分けだが、
`REQ-C10` の展開時に重複を検査する。

---

## 10. REQ-C10 批判的検証ワークスペース【完全展開】

**論文の主張を鵜呑みにせず、自分で確かめるための場である。**
`REQ-C07` が証拠を記録し、`REQ-C08` が由来を繋ぐのに対し、本要求は**主張そのものを検証する。**

### 10.1 上位要求

| カテゴリ | 批判的検証（`VERIFY`） |
|---|---|
| **要求 `REQ-C10`** | システムは重要な主張について、主張区分・前提条件・成立範囲・実験条件・支持と反証・矛盾・不足情報・手計算・反例・公式実装・再現結果を**関連付け**、人間が最終判断を**承認できるようにする**ものとする。 |
| **理由** | 論文の主張が正しいかを LLM だけで確定させると、誤りを誤りのまま通す。人間が判断するための材料を揃えることが機能であり、判断そのものを代行しない。 |
| **説明** | 実装単位は `F-19`（ワークスペース）、`B-12`（証拠台帳）、`B-13`（プロヴェナンス管理）。**主要な局面はサブ要求4件が担う。** |

**動詞の数を数える。** 「関連付ける」「承認できるようにする」の2つ。
8個未満なので**分割しないでよい**（REF-17 4.2.2）。

⚠️ **本要求は既存サブ要求4件が主要な局面を担う。** 新設する下位要求は1件のみとする。

| 局面 | 担当 |
|---|---|
| 主張の前提・帰結・成立範囲の分離 | **`REQ-C10-S01`**（既存） |
| 実験条件の構造化 | **`REQ-C10-S02`**（既存） |
| 支持例・反例・検証コードの作成 | **`REQ-C10-S03`**（既存） |
| 記述の区分と直接支持範囲の分離 | **`REQ-C10-S04`**（既存） |
| **関連付けと最終判断の承認** | `REQ-C10-R1`（新設） |

### 10.1.1 ⚠️ `REQ-C07`・`REQ-C08` との境界

本要求は `B-12` を `REQ-C07` と、`B-13` を `REQ-C08` と共有する。
**両方とも展開済みであるため、条項単位で照合した。**

| 観点 | `REQ-C07` | `REQ-C08` | **本要求** |
|---|---|---|---|
| 扱う対象 | **情報源** | **由来の連鎖** | **主張そのもの** |
| 記録するもの | 情報源の属性・一致状況・確信度 | 入力から成果物への変換 | 主張の前提・帰結・成立範囲・実験条件 |
| 矛盾の扱い | **情報源どうしの食い違い**を保持 | — | **主張と証拠の食い違い**を検討状態として保持 |
| 人間の役割 | どの情報源を**採用する**か | 訂正の影響範囲を**判断する** | 主張の真偽を**最終判断する** |

**重複は無い。** ただし2件の注意がある。

⚠️ **`REQ-C10-S02`（実験条件の構造化）と `REQ-C07-R1`（情報源の属性の記録）は近い。**
前者は**論文が報告した実験条件**を構造化し、後者は**その情報をどこから得たか**を記録する。
**対象が違う。**

⚠️ **`REQ-C10-S02` と `REQ-C05-S02`（実行の再現条件の記録）も近い。**
[`crosswalk-01-reproduction.md`](requirements-analysis/crosswalk-01-reproduction.md) が
「記録する項目は同一だが向きが逆」と整理している。
**前者は論文に書かれた条件を読み取り、後者は自分の実行条件を記録する。**

---

### 10.2 既存サブ要求 `REQ-C10-S01` 主張の前提と帰結を分離する

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは重要な主張ごとに、前提条件、帰結、成立範囲、省略された推論および読解深度を分離して記録し、利用者が必要性と理解度に応じて読解深度を変更できるものとする。 |
| **受入基準**（同上） | 主張を原文へ関連付け、前提・帰結・成立範囲・読解深度を分離し、未解決を保持し、**LLMだけで導出確認済みまたは理解完了にできない**。 |

**＜主張の分離＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S01.10` | 重要な主張を抽出し、原文の該当箇所へ関連付ける。 |
| `REQ-C10-S01.20` | 主張ごとに、前提条件・帰結・成立範囲を分けて記録する。 |
| `REQ-C10-S01.30` | 論文が省略した推論の段を、省略として記録する。 |
| `REQ-C10-S01.40` | 分離できなかった要素を未解決として保持する。**推測で補わない。** |

**＜読解深度＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S01.50` | 主張ごとに読解深度を記録する。 |
| `REQ-C10-S01.60` | 利用者が読解深度を変更できる。**必要性と理解度に応じて上下できる。** |
| `REQ-C10-S01.70` | 深度の変更履歴を保持する。 |

**＜⚠️ 自動確定の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S01.80` | LLM の出力だけで、省略された推論を**導出確認済み**にしない。 |
| `REQ-C10-S01.90` | LLM の出力だけで、主張を**理解完了**にしない。 |
| `REQ-C10-S01.100` | 導出確認済みと理解完了は、**利用者の明示的な操作でのみ確定する。** |

---

### 10.3 既存サブ要求 `REQ-C10-S02` 実験条件を構造化する

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは実験結果ごとに、データ、前処理、モデル、ハイパーパラメータ、乱数、試行回数、評価指標、エラーバー、計算資源および比較条件を構造化し、**記載済み、未報告、推定、外部確認済みを区別する**ものとする。 |
| **受入基準**（同上） | 複数手法の実験条件を比較し、未報告を未確認として保持し、注意事項だけで結果を無効と断定せず、利用者の判断と根拠を保存できる。 |

**＜条件の構造化＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S02.10` | 実験結果ごとに、データ・前処理・モデル・ハイパーパラメータを記録する。 |
| `REQ-C10-S02.20` | 乱数・試行回数・評価指標・エラーバー・計算資源を記録する。 |
| `REQ-C10-S02.30` | 比較条件を記録する。複数手法を比較した実験では、条件が揃っているかを確かめられるようにする。 |

**＜⚠️ 出所の区別＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S02.40` | 各項目の出所を、記載済み・未報告・推定・外部確認済みの4つに区別する。 |
| `REQ-C10-S02.50` | **未報告を推定で埋めない。** 未報告のまま保持する。 |
| `REQ-C10-S02.60` | 推定した項目には、推定の根拠を添える。 |
| `REQ-C10-S02.70` | 外部確認済みの項目には、確認先を添える。 |

> **`S02.40` の4区分を仕様にした理由。**
> 「記載済み」と「未報告」だけなら、**推定した値が記載済みに混ざる。**
> 「推定」と「外部確認済み」を分けなければ、後から誰がその値を決めたのかを追えない。
> `REQ-C05-S03.40`（性能差を断定できない場合は不明と記録する）と同じ思想である。

**＜複数手法の比較＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S02.80` | 複数手法の実験条件を並べて比較できる。 |
| `REQ-C10-S02.90` | 条件が揃っていない比較を、そう分かる形で示す。 |
| `REQ-C10-S02.100` | **注意事項があるだけで結果を無効と断定しない。** 利用者が判断する。 |
| `REQ-C10-S02.110` | 利用者の判断とその根拠を保存する。 |

---

### 10.4 既存サブ要求 `REQ-C10-S03` 支持例と反例を作る

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは重要な主張または前提に対し、支持例、最小具体例、仮定が成立しない反例、境界条件、手計算および小規模な検証コードを作成・保存し、対象主張、変更・維持した条件、予想、入力、手順、観察結果、適用範囲および未確認範囲を記録する。**検証結果から派生した仮説または設計案を確定済み結論と区別する**ものとする。 |
| **受入基準**（同上） | 検証成果物を主張・前提・数式へ関連付け、支持例と反例の条件、予想と観察、適用範囲外と反証、仮説と検証済み結論を区別する。**一例だけで一般的真偽を確定せず、サンドボックスと承認を迂回しない**。 |

**＜検証成果物の作成＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S03.10` | 主張または前提に対し、支持例・最小具体例・反例・境界条件を作成できる。 |
| `REQ-C10-S03.20` | 手計算の問題と、小規模な検証コードを作成できる。 |
| `REQ-C10-S03.30` | 作成した検証成果物を、対象の主張・前提・数式へ関連付ける。 |

**＜記録する内容＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S03.40` | 変更した条件と維持した条件を、分けて記録する。 |
| `REQ-C10-S03.50` | 実行前の予想と、実行後の観察結果を、分けて記録する。 |
| `REQ-C10-S03.60` | 入力と手順を記録し、再実行できる状態にする。 |
| `REQ-C10-S03.70` | 適用範囲と未確認範囲を記録する。 |

**＜⚠️ 区別の維持＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S03.80` | 検証結果から派生した仮説を、確定済みの結論と区別して記録する。 |
| `REQ-C10-S03.90` | 適用範囲外であることと、反証されたことを区別する。 |
| `REQ-C10-S03.100` | **一例だけで一般的な真偽を確定しない。** 反例1件で主張全体を否定しない。 |

⚠️ **`S03.90` は誤りやすい。** 「主張が成り立たなかった」には2つの場合がある。
**主張が適用されない条件で試した**のか、**適用される条件で成り立たなかった**のか。
前者は主張を否定せず、後者は否定する。区別しなければ、誤った反証が積み上がる。

**＜実行の制約＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S03.110` | 検証コードの実行は `REQ-C06` の隔離と承認に従う。**迂回しない。** |
| `REQ-C10-S03.120` | 検証成果物を `DM-E12 Provenance` へ関連付けて保存する。 |

---

### 10.5 既存サブ要求 `REQ-C10-S04` 記述を区分する

⚠️ **本節は既存サブ要求である。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは重要な記述を事実、実験結果、解釈・推論、仮説、予測、宣伝的・価値判断的表現へ区分し、根拠が直接支持する範囲と追加の推論を要する範囲を分けるものとする。誤り、誇張、バイアス等は根拠付きの検討状態として表示し、人間が訂正・承認でき、**LLMだけで真偽や不正を確定しない**ものとする。 |
| **受入基準**（同上） | 原文位置、主張区分、直接支持範囲、根拠種別、検討状態、分類主体を表示し、利用者の訂正履歴を保持できる。**査読・人物属性だけで真偽やバイアスを確定せず、LLM単独で真偽、不正または検討完了へ変更できない**。 |

**＜記述の区分＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S04.10` | 重要な記述を、事実・実験結果・解釈と推論・仮説・予測・宣伝的表現の6つに区分する。 |
| `REQ-C10-S04.20` | 区分ごとに、原文の該当位置を示す。 |
| `REQ-C10-S04.30` | 区分の主体（LLM か利用者か）を記録する。 |

**＜直接支持範囲＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S04.40` | 根拠が直接支持する範囲と、追加の推論を要する範囲を分けて示す。 |
| `REQ-C10-S04.50` | 根拠の種別を示す。 |

**＜検討状態＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S04.60` | 誤り・誇張・バイアスの疑いを、**確定ではなく検討状態として**表示する。 |
| `REQ-C10-S04.70` | 検討状態に、その根拠を添える。 |
| `REQ-C10-S04.80` | 利用者が検討状態を訂正できる。訂正の履歴を保持する。 |

**＜⚠️ 自動確定の禁止＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-S04.90` | 査読の有無だけで、真偽やバイアスを確定しない。 |
| `REQ-C10-S04.100` | 著者の属性だけで、真偽やバイアスを確定しない。 |
| `REQ-C10-S04.110` | LLM 単独で、真偽・不正・検討完了のいずれへも変更しない。 |
| `REQ-C10-S04.120` | 検討完了は、**利用者の明示的な操作でのみ確定する。** |

---

### 10.6 下位要求 `REQ-C10-R1` 関連付けて最終判断を受け取る

| 項目 | 内容 |
|---|---|
| **要求** | システムは主張とその検証に関わる要素を1つの画面へ**集め**、要素どうしの関連を**示し**、人間の最終判断を**受け取って記録する**ものとする。 |
| **理由** | 材料が別々の場所にあると、突き合わせる作業に手間がかかり、判断が浅くなる。判断を受け取っても記録しなければ、後から見直せない。 |
| **説明** | サブ要求4件が記録した内容を集約する局面である。実装単位は `F-19`（批判的検証ワークスペース）。 |

**＜要素の集約＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-R1.10` | 1つの主張について、主張区分・前提条件・成立範囲・実験条件を1つの画面へ集める。 |
| `REQ-C10-R1.20` | 同じ画面へ、支持例・反例・境界条件・手計算・検証コードの結果を集める。 |
| `REQ-C10-R1.30` | 同じ画面へ、公式実装の該当箇所と再現結果を集める。 |
| `REQ-C10-R1.40` | 同じ画面へ、未解決の矛盾と不足情報を集める。 |

**＜関連の提示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-R1.50` | 要素どうしの関連を示す。どの証拠がどの主張を支持するかを辿れるようにする。 |
| `REQ-C10-R1.60` | 関連の無い要素を、無いものとして示す。**推測で関連を作らない。** |

**＜⚠️ 最終判断＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C10-R1.70` | 主張に対する最終判断を、利用者が入力できる。 |
| `REQ-C10-R1.80` | **LLM が最終判断を代行しない。** 承認は人間の明示的な操作に限る。 |
| `REQ-C10-R1.90` | 判断の理由を利用者が入力できる。**理由を空のまま判断を確定できない。** |
| `REQ-C10-R1.100` | 判断と理由と日時を記録する。 |
| `REQ-C10-R1.110` | 判断を後から変更できる。変更を追記として残し、**上書きしない。** |

---

### 10.7 動詞と仕様の対応（漏れの検査）

| 上位要求の動詞 | 対応する仕様 |
|---|---|
| 検証に関わる要素を**関連付ける** | `S01.10`〜`S01.70`、`S02.10`〜`S02.110`、`S03.10`〜`S03.120`、`S04.10`〜`S04.80`、`R1.10`〜`R1.60` |
| 人間が最終判断を**承認できるようにする** | `R1.70`〜`R1.110`、`S01.80`〜`S01.100`、`S04.90`〜`S04.120` |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 主張区分と原文根拠を表示する | `S04.10`、`S04.20`、`S01.10` |
| 成立条件・支持反証・矛盾・手計算・反例を関連付ける | `S01.20`、`S03.10`〜`S03.30`、`R1.10`〜`R1.50` |
| 公式実装・再現結果を関連付ける | `R1.30` |
| LLMではなく人間が最終判断を承認できる | `R1.80`、`S01.80`〜`S01.100`、`S04.110`、`S04.120` |

### 10.8 自動確定の禁止が置かれた箇所

**本要求は「LLM だけで確定しない」を4箇所で定めている。** 対象が異なるため、まとめない。

| 仕様 | 何を確定させないか |
|---|---|
| `S01.80`・`S01.90` | 省略された推論の**導出確認済み**、主張の**理解完了** |
| `S02.100` | 注意事項だけによる結果の**無効判定** |
| `S03.100` | 一例による**一般的真偽** |
| `S04.90`〜`S04.120` | 査読・著者属性による**真偽とバイアス**、LLM 単独の**検討完了** |

⚠️ **「人間が最終判断する」を1行で書くと、どの判断を指すのかが定まらない。**
判断の種類ごとに禁止を明示する。

---

## 11. REQ-C09 論文ポートフォリオ【完全展開】

**1本の論文で完結せず、蓄積して次に活かすための要求である。**

### 11.1 上位要求

| カテゴリ | 蓄積と対話（`ARCHIVE`） |
|---|---|
| **要求 `REQ-C09`** | システムは論文の更新・学習履歴・関連論文・獲得スキル・復習・通知・知識再利用を統合した論文ポートフォリオを**提供する**ものとする。 |
| **理由** | 1本ずつ読んで終わりでは、読んだ量が力にならない。前に読んだものとの関係が見えて初めて、蓄積が次の理解を助ける。 |
| **説明** | **段階1→4で拡張する要求である。** 初期リリースは段階1に相当する範囲のみを実装する。実装単位は `F-13`（プロジェクト管理）、`F-18`（学習ポートフォリオ）、`B-09`（台帳・PJ 永続化）、`B-14`（論文更新・学習履歴）、`N-08`（学習データのプライバシー）。 |

**動詞の数を数える。** 「提供する」の1つ。
ただし提供する対象が7要素（論文更新・学習履歴・関連論文・獲得スキル・復習・通知・知識再利用）に分かれるため、
**対象による分割**を行う。

⚠️ **主要な局面は既存サブ要求3件が担う。** 新設する下位要求は無い。

| 局面 | 担当 |
|---|---|
| プロジェクトの保存と再開 | **`REQ-C09-S01`**（既存） |
| 関連知識の候補化と深掘りキュー | **`REQ-C09-S02`**（既存） |
| 公開範囲と外部送信前の確認 | **`REQ-C09-S03`**（既存） |

### 11.1.1 ⚠️ 段階の定義と本章の範囲

[`requirements.md`](requirements.md) 第8章が定める初期リリースの一覧に、
**本要求の実装単位のうち含まれるのは `B-09` だけである。**

| 実装単位 | 初期リリース |
|---|---|
| `B-09` 台帳・PJ 永続化 | ✅ 含む |
| `F-13` プロジェクト管理 | ✖ 含まない |
| `F-18` 学習ポートフォリオ | ✖ 第2フェーズ以降 |
| `B-14` 論文更新・学習履歴 | ✖ 第2フェーズ以降 |
| `N-08` 学習データのプライバシー | ✖ 第2フェーズ以降 |

| 段階 | 提供する範囲 | 本章での扱い |
|---|---|---|
| **段階1** | 論文プロジェクトの保存・再開 | ✅ **仕様まで展開する** |
| 段階2 | 論文更新の差分、深掘りキュー | 枠のみ |
| 段階3 | 学習履歴、獲得スキル、復習 | 枠のみ |
| 段階4 | 通知、知識再利用、公開範囲の管理 | 枠のみ |

**本章が仕様まで落とすのは段階1に限る**（`REQ-C08` 第9.1.1節と同じ方針）。
段階2〜4は何が増えるかを枠として示すが、**中身が未確定であるため仕様化しない。**

### 11.1.2 ⚠️ `REQ-C02-S01`（`DM-E18 LearningProfile`）との境界

本要求は学習履歴を扱い、`REQ-C02-S01` は既読資料をプロジェクト横断で保持する。
**領域が重なるため、境界を定める。**

| | `REQ-C02-S01`（`DM-E18`） | **本要求（段階3以降）** |
|---|---|---|
| 保持するもの | **「読んだ」「受講した」という事実** | 学習の履歴、獲得したスキル、復習の対象 |
| 目的 | **前提知識の重複説明を省く** | 蓄積を次の理解へ活かす |
| 理解度 | **保持しない** | 段階3の定義に含まれるため、扱いを別途決める |

⚠️ **`DM-E18` は理解度を保持しない**（第6バッチ選択肢3）。
一方、本要求の段階3は「学習履歴、獲得スキル、復習」を含み、**獲得スキルは理解度に近い。**
**段階3を展開するとき、第6バッチの判断と衝突しないかを検査する。**

---

### 11.2 既存サブ要求 `REQ-C09-S01` プロジェクトを保存し再開する

⚠️ **本節は既存サブ要求であり、段階1に相当する。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは論文ファイル、注釈、計算、メモ、読書位置および対象版を論文プロジェクトとして保存し、検索、再表示、バックアップ、復元、移行可能なエクスポートおよび確認付き削除を可能にするものとする。 |

**＜保存する対象＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C09-S01.10` | 論文ファイル・注釈・計算・メモを、論文プロジェクトとして保存する。 |
| `REQ-C09-S01.20` | 読書位置を保存し、再開したとき同じ位置から続けられるようにする。 |
| `REQ-C09-S01.30` | 対象版を保存する。**どの版を読んでいたかを失わない。** |

**＜再開＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C09-S01.40` | セッションが切れた場合も、保存済みの状態から再開できる。 |
| `REQ-C09-S01.50` | 再開したとき、進行中だった工程と現在地を示す。 |

**＜検索と再表示＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C09-S01.60` | 保存したプロジェクトを検索できる。 |
| `REQ-C09-S01.70` | 過去のプロジェクトを再表示できる。 |

**＜バックアップと移行＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C09-S01.80` | プロジェクトをバックアップし、復元できる。 |
| `REQ-C09-S01.90` | 他の環境へ移行できる形式でエクスポートできる。 |
| `REQ-C09-S01.100` | エクスポートに、対象版と取得日時を含める。 |

**＜削除＞**

| 仕様 ID | 仕様 |
|---|---|
| `REQ-C09-S01.110` | プロジェクトの削除は、**利用者の明示的な確認を経て行う。** |
| `REQ-C09-S01.120` | 削除の対象と範囲を、実行前に示す。 |

---

### 11.3 既存サブ要求 `REQ-C09-S02` 関連知識を候補化し深掘りキューで管理する

⚠️ **本節は既存サブ要求であり、段階2に相当する。**
実装単位 `B-14` が第2フェーズ以降であるため、**仕様は展開しない。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは、参考文献、関連論文、データセットおよび関連実装を、対象論文との関係、引用による影響、影響の時間変化、推薦および利用者の関心から候補化し、深掘りキューとして管理するものとする。 |
| **状態** | **段階2。仕様は第2フェーズで展開する。** |

⚠️ **突合04で `Z-01`（関連研究の時系列的位置づけ）が本サブ要求へ吸収されている。**
`PR-M-04`・`ARF-M-10`・`PR-M-12` の3候補が「対象論文との関係、引用による影響、影響の時間変化」に対応する。
**展開時に、この3候補の内容を仕様へ落とす。**

---

### 11.4 既存サブ要求 `REQ-C09-S03` 公開範囲を選び外部送信前に確認する

⚠️ **本節は既存サブ要求であり、段階4に相当する。**
実装単位 `N-08`・`N-10` が第2フェーズ以降であるため、**仕様は展開しない。**

| 項目 | 内容 |
|---|---|
| **要求**（[`requirements.md`](requirements.md) 第3.2節より） | システムは自己説明、計算、注釈および成果物の公開範囲を非公開、限定共有、公開から選択でき、外部送信前に機密情報、個人情報、シークレット、著作権、引用元およびライセンスを確認するものとする。 |
| **状態** | **段階4。仕様は第2フェーズで展開する。** |

⚠️ **突合04で `AR-M-02`（出典の明示・生成AIの関与・著作物の点検）が本サブ要求の 🔨 と判定されている。**
また `ARF-M-08`（貢献の記録）が主題3として5択未作成のまま残っている。
**展開時に、両者との関係を整理する。**

⚠️ **`REQ-C11`（質問台帳と外部対話）も外部送信を扱う。** 本サブ要求が公開範囲と送信前確認を、
`REQ-C11` が対話の記録を担うという切り分けだが、**`REQ-C11` の展開時に重複を検査する。**

---

### 11.5 段階2〜4で増える要素

上位要求が挙げる7要素のうち、段階1で扱うのは**プロジェクトの保存と再開**だけである。
残る要素は段階2以降で追加する。

| 要素 | 段階 | 担当 |
|---|---|---|
| 論文更新 | 2 | `REQ-C09-S02` および `B-14` |
| 関連論文 | 2 | `REQ-C09-S02` |
| 学習履歴 | 3 | 未定 |
| 獲得スキル | 3 | 未定。⚠️ 11.1.2節の境界に注意 |
| 復習 | 3 | 未定 |
| 通知 | 4 | 未定 |
| 知識再利用 | 4 | 未定 |

⚠️ **段階3・4の担当は未定である。** 新しいサブ要求を設けるか、既存のどれかへ含めるかを、
**段階を進めるときに決める。**

---

### 11.6 動詞と仕様の対応（漏れの検査）

| 上位要求の対象 | 段階 | 対応する仕様 |
|---|---|---|
| プロジェクトの保存と再開 | 1 | `S01.10`〜`S01.120` |
| 論文更新・関連論文 | 2 | 未展開（`S02`） |
| 学習履歴・獲得スキル・復習 | 3 | 未展開 |
| 通知・知識再利用・公開範囲 | 4 | 未展開（`S03`） |

**受入基準との対応。**

| 受入基準 | 対応する仕様 |
|---|---|
| 1→4の各段階を検証できる | 段階の定義は11.1.1節。検証の仕様は `REQ-C08-R4.70`・`R4.80` が担う |
| 更新差分を管理できる | 未展開（段階2） |
| 学習履歴を管理できる | 未展開（段階3） |
| 関連知識を管理できる | 未展開（段階2、`S02`） |
| 同意・出力・削除に対応できる | 出力と削除は `S01.90`〜`S01.120`。**同意は未展開**（段階4、`N-08`） |

⚠️ **受入基準の5項目のうち、段階1で満たせるのは「出力と削除」の一部だけである。**
残りは段階2以降で満たす。**初期リリースの検証では、この受入基準の全体を求めない。**

---

## 12. 他の要求【要求と理由まで】

以下は USDM の要求文へ書き直したもの。**仕様は未展開である。**
第13章が定めた順序で展開する。展開が済んだ要求は本章から外し、独立した章へ移す。
⚠️ REQ-C01（第2章）、REQ-C03（第3章）、REQ-C05（第4章）、REQ-C02（第5章）、REQ-C04（第6章）、REQ-C06（第7章）、REQ-C07（第8章）、REQ-C08（第9章）、REQ-C10（第10章）、REQ-C09（第11章）は展開済みのため、本章に含まない。

### 12.1 蓄積と対話（`ARCHIVE`）

| ID | 要求（振る舞い） | 理由 |
|---|---|---|
| `REQ-C11` | 未解決の疑問を**溜め**、生成AI・共同利用者・専門家・著者から得た回答を**受け取り**、回答の主体と情報源を**区別して記録し**、外部への送信を人間の操作に**限る**。 | 誰が答えたかで、その回答の重みは変わる。AI の回答を人間の回答と同列に扱うと、根拠のない断定が混ざる。また、外部へ何かを送る操作は取り消せない。 |

---

## 13. 未解決の確認事項

- **正本をどちらにするか。** 本書が全要求を展開し終えた時点で、
  [`requirements.md`](requirements.md) v0.2 の第3章を本書への参照に置き換えるか、
  両方を維持するかを決める。両方を維持する場合、同期の規約が要る。
- ~~**展開の順序。**~~ → **決定**（2026年9月3日）。次の順で展開する。

  1. `REQ-C03` 論文の取り込み — **論文が入らなければ他の工程が始まらない**
  2. `REQ-C02`・`REQ-C04` 読解・学習 と `REQ-C05` 再現実装 — `REQ-C01` が両者を**同等の主要経路**と定めるため、片方だけ先行させない
  3. `REQ-C06` 実行言語と承認ゲート
  4. `REQ-C07`・`REQ-C08` 証拠と由来 — 何を記録するかが決まってから
  5. `REQ-C09`〜`REQ-C11` — **第8章が段階2以降を第2フェーズへ先送りしている**ため最後

  ⚠️ **1回の作業単位は下位要求1件とする。** `REQ-C01-R1` の実測で仕様11件・約35行。
- ~~**サブ要求12件の扱い。**~~ → **解決済み**（2026年9月3日、[`requirements-decisions/batch-07-options.md`](requirements-decisions/batch-07-options.md) 選択肢3）。
  既存サブ要求はIDを保ったまま USDM 下位要求と同じ階層に置き、仕様を `REQ-Cxx-Sxx.10` と付番する。
  新規の下位要求だけ `Rn` を使う。**現在のサブ要求は15件。**
- **非機能要求の書き方。** REF-17 2.2.9 は非機能要求も要求仕様書へ含めるとする。
  `N-01`〜`N-10` を USDM の要求として書き直すかどうか。
