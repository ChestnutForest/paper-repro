# 判断材料：要求文の正本をどちらにするか

- 対象プロダクト: `paper-repro`
- 文書種別: 判断材料の整理（判断そのものは行っていない）
- 作成日: 2026年9月5日
- 契機: [`../requirements-usdm.md`](../requirements-usdm.md) 1.2節が
  「本書が全要求を展開し終えるまで、両方を維持する。展開が済んだ時点で、どちらを正本とするかを判断する」
  と定め、**2026年9月4日に展開が完了した**こと
- 状態: **反映待ち**（材料のみ。選択肢の決定は次回）
- 影響先: [`../requirements.md`](../requirements.md) 第3章、
  [`../requirements-usdm.md`](../requirements-usdm.md) 第13章の未解決事項

---

## 0. 本書が決めていないこと

⚠️ **本書は正本を決めない。** 決めたのは、判断に必要な数値と比較の軸だけである。

---

## 1. 実測した数値

### 1.1 両文書の規模

| | [`requirements.md`](../requirements.md) | [`requirements-usdm.md`](../requirements-usdm.md) |
|---|---|---|
| 全体 | **904行** | **2429行** |
| 要求文 | 27行前後（受入基準と実装単位は requirements.md にのみ存在。1.6節を参照） | 27行前後（要求文のみ） |
| 固有の内容 | 761行（84%） | 2400行前後（仕様393件） |

**重複しているのは143行。** 両文書を合わせた3333行のうち **4%** である。

### 1.2 `requirements.md` の章構成

**第3章以外は USDM に存在しない。**

| 章 | 開始行 | 内容 |
|---|---|---|
| ID体系 | 24 | `REQ-Cxx`・`Sxx`・実装単位の体系 |
| 0 | 43 | 用語定義：「再現」の水準（REF-10 の3水準） |
| 1 | 75 | ツールの目的とスコープ、工程モデルの確定 |
| 2 | 125 | 全体アーキテクチャ |
| **3** | **200** | **確定要求（重複箇所）** |
| 4 | 343 | フロントエンド要件（`F-xx`） |
| 5 | 387 | バックエンド要件（`B-xx`） |
| 6 | 426 | データモデル（**362行。第3章の2.5倍**） |
| 7 | 788 | 最大の技術的リスク：サンドボックス実行 |
| 8 | 810 | 初期リリース範囲 |
| 9 | 845 | 要求と実装単位の対応表 |
| 10 | 885 | 未決事項 |

⚠️ **`requirements.md` は要求定義書ではない。** 製品の設計全体を束ねる文書である。

### 1.3 参照の分布

```powershell
git grep -c "requirements.md" -- docs/ AGENTS.md CLAUDE.md README.md scripts/
git grep -c "requirements-usdm" -- docs/ AGENTS.md CLAUDE.md README.md scripts/
```

| | 参照しているファイル数 |
|---|---|
| `requirements.md` | **58ファイル** |
| `requirements-usdm.md` | **16ファイル** |

**3.6倍の差がある。** 中身も異なる。

| 参照元 | `requirements.md` | `requirements-usdm.md` |
|---|---|---|
| 設計文書（`arc-*`・`product-design`・`roadmap`） | 多数 | `arc-*` の6件のみ |
| 要求検討（`requirements-analysis/`） | 20ファイル | 0 |
| 選択記録（`requirements-decisions/`） | 7ファイル | `batch-07` のみ |
| 開発の入口（`AGENTS.md`・`CLAUDE.md`・README） | あり | なし |

⚠️ **`requirements.md` は既にプロジェクトのハブである。**
`AGENTS.md` の「作業開始時の読み順」に含まれる。

⚠️ **USDM はまだ参照されていない。** 2429行・393仕様を書いたが、
設計文書からの参照は6件にとどまる。**新しいためであり、価値が低いからではない。**

---

### 1.4 ⚠️ 重複は完全な複製ではない

143行の重複は、**同じ内容を2箇所に書いたもの**ではない。
文体統一（`88c6a09`）を経てなお、**末尾の表現が食い違う要求がある。**

`REQ-C10` を例に取る。

| 文書 | 末尾 |
|---|---|
| `requirements.md` | 「…人間が最終判断を承認できる**批判的検証ワークスペースを提供する**ものとする」 |
| `requirements-usdm.md` | 「…人間が最終判断を**承認できるようにする**ものとする」 |

前者は**実装形態**（ワークスペース）で締め、後者は**振る舞い**で締めている。

⚠️ **USDM の形が適切である。** 「ワークスペース」は画面の形であって要求ではない。
実装単位 `F-19`（批判的検証ワークスペース）がその形を担っており、
要求文が形まで指定すると、**別の形で同じ振る舞いを実現する選択肢を狭める。**

#### 1.4.1 この事実が判断に与える影響

**選択肢2（両方を維持し、同期の規約を定める）が弱くなる。**

3.1節の軸1では「片方だけ直したときに矛盾する」ことを問題とした。
しかし実際には、**同期する以前に一致していない。**

⚠️ **他の要求にも同様の食い違いがある可能性がある。未検査である。**
選択肢2を採るなら、規約を定める前に**27件すべてを突き合わせて差を洗い出す**必要がある。
その作業量は、選択肢3や4を採る場合と変わらないかもしれない。

---

### 1.5 11件の要求文をすべて突き合わせた結果

1.4節で `REQ-C10` の食い違いを見つけたため、**メイン要求11件すべてを突き合わせた**（2026年9月5日）。

**11件中10件が不一致だった。うち6件は表記ではなく中身に関わる。**

| 要求 | 差の性格 | 何が違うか |
|---|---|---|
| `REQ-C01` | ⚠️ **中身** | 「**同等の主要経路として**」が USDM に無い |
| `REQ-C02` | ⚠️ **中身** | 「**前提知識を利用条件とせず**」が USDM の要求文に無い（仕様 `R1.40` へ移動） |
| `REQ-C03` | ⚠️ **別の文** | `requirements.md` は対象範囲（arXiv が中核、他は拡張）、USDM は取り込みの振る舞い |
| `REQ-C04` | 粒度のみ | 名詞の羅列を動詞へ開いた |
| `REQ-C05` | ⚠️ **中身** | 「**支援範囲とする**」が USDM に無い |
| `REQ-C06` | ⚠️ **中身** | 「**Python を初期実行対象とし**」「**段階追加し**」が USDM の要求文に無い |
| `REQ-C07` | 粒度 | 「確信度」が USDM の要求文に無い（仕様 `R1.90` にはある） |
| `REQ-C08` | 段階性・粒度 | 「**最終的に**」が無い。「双方向に」を「根拠から成果物へも成果物から根拠へも」と展開 |
| `REQ-C09` | 段階性 | 「**最終的に**」が無い |
| `REQ-C10` | ⚠️ **中身** | 末尾が「批判的検証ワークスペースを提供する」対「承認できるようにする」 |
| `REQ-C11` | なし | 表記のみ（読点が中黒、動詞の強調） |

#### 1.5.1 ⚠️ 消えているのは一方向である

**11件すべてで、消えているのは `requirements.md` 側にあって USDM に無いものである。**
逆（USDM にあって `requirements.md` に無い）は1件も無かった。

消えた語を並べると性格が見える。

| 消えた語 | 何を示す語か |
|---|---|
| 同等の主要経路として（`REQ-C01`） | **方針**。読解と再現実装のどちらも従属させない |
| 前提知識を利用条件とせず（`REQ-C02`） | **方針**。学びたい人を排除しない |
| 初期に arXiv を中核とし（`REQ-C03`） | **範囲**。初期リリースの対象 |
| 支援範囲とする（`REQ-C05`） | **限定**。製品が代行するのではなく支援に留まる |
| Python を初期実行対象とし（`REQ-C06`） | **範囲**。段階追加の起点 |
| 最終的に（`REQ-C08`・`REQ-C09`） | **段階性**。1→4で拡張する要求であること |

**いずれも範囲・方針・限定を示す語であり、振る舞いではない。**

USDM は要求文を振る舞いの記述に絞った。その結果、**方針が要求文から抜けた。**

⚠️ **失われてはいない。** 仕様（`REQ-C02-R1.40` など）や説明欄（「段階1→4で拡張する要求である」）
には残っている。**しかし要求文だけを読んでも分からない。**

#### 1.5.2 この結果が判断に与える影響

**選択肢2（両方を維持し、同期の規約を定める）は事実上落ちる。**

同期すべき対象が、**11件中10件で一致していない。**
規約を定める前に、**11件それぞれについて「どちらが正しいか」を決め直す**必要がある。
その作業量は、選択肢3や4を採る場合と変わらない。

**選択肢3（役割を分ける）と選択肢4（受入基準だけ移す）の比較が残る。**

⚠️ **ただし本節の結果は、選択肢4にも影響する。**
選択肢4は「要求文は `requirements.md` に残し、受入基準だけ USDM へ移す」案だが、
**その要求文が既に2つある。** 移す前に、どちらの要求文を残すかを11件分決める必要がある。

#### 1.5.3 ⚠️ 未検査

**サブ要求15件は突き合わせていない。** 本節はメイン要求11件のみを対象とした。
サブ要求は表形式で記述されており、比較の形が変わる。

メイン要求で10件が不一致だった以上、**サブ要求にも差がある見込みが高い。**

---

### 1.6 ⚠️ 1.1節と2.4節の訂正

2026年9月5日、両文書の構成を詳しく確認したところ、**本書の記述に2つの誤りが見つかった。**

#### 1.6.1 訂正1：重複は143行ではなく、要求文27行前後である

1.1節は「要求文と受入基準 143行（第3章、200〜342行）」と書いたが、**誤りである。**

第3章の143行には次が含まれる。

| 内容 | USDM にあるか |
|---|---|
| 要求文（メイン11件・サブ15件） | **ある**（重複） |
| **受入基準** | **無い** |
| **実装単位との対応** | **無い** |

**USDM が持っているのは要求文だけである。**
受入基準と実装単位の対応は `requirements.md` にしか存在しない。

**したがって重複は要求文27行前後、両文書3333行の1%以下である。**

⚠️ **この訂正は判断を大きく変える。**
143行の重複を前提に「同期の規約が要る」「一行要約へ縮小する」と論じたが、
**27行ならどちらも釣り合わない。**

#### 1.6.2 訂正2：選択肢4は成立しない

2.4節は「受入基準だけを USDM へ移す」案としたが、**移す対象が存在しない。**

USDM は受入基準を持たない。持っているのは**受入基準と仕様の対応表**であり、
受入基準の文言は `requirements.md` から参照している。

```powershell
Select-String -Path docs\requirements-usdm.md -Pattern "受入基準との対応"
```

**10箇所ある。** 11要求のうち `REQ-C01` だけが持たない（第2章は8月29日に別途書かれたため）。

⚠️ **11件中10件にあって1件に無いのは不整合である。** 正本の判断とは別に解消する。

#### 1.6.3 実態：既に役割は分かれている

| | 持っているもの |
|---|---|
| [`requirements.md`](../requirements.md) | 要求文、**受入基準**、実装単位との対応、データモデル、初期リリース範囲 |
| [`requirements-usdm.md`](../requirements-usdm.md) | 要求文（重複）、下位要求、**仕様393件**、受入基準との対応表（10件） |

**重複しているのは要求文だけである。**

---

### 1.7 選択肢5：役割分担を明記し、要求文の食い違いだけ解消する

1.6節の訂正を踏まえると、**選択肢1〜4のどれとも違う案が最も実態に近い。**

#### 1.7.1 内容

**文書の統合も縮小も行わない。** 次の2つだけを行う。

1. **役割分担を両文書へ明記する。**
   `requirements.md` は要求文・受入基準・実装単位との対応を持つ。
   `requirements-usdm.md` は要求文・下位要求・仕様を持つ。
   **重複するのは要求文のみであり、それは意図的な冗長である。**

2. **要求文27行の食い違いを解消する**（1.5節の6件）。
   `REQ-C01`（同等の主要経路）、`REQ-C02`（利用条件とせず）、`REQ-C03`（別の文）、
   `REQ-C05`（支援範囲とする）、`REQ-C06`（Python が初期対象）、`REQ-C10`（末尾）。

#### 1.7.2 なぜ他の選択肢より優るか

| 選択肢 | 落ちる理由 |
|---|---|
| 1 参照に置換 | 27行を削るために58ファイルの参照の意味を変える。釣り合わない |
| 2 同期の規約 | 27行なら規約を設けるほどではない。**食い違いを直せば済む** |
| 3 一行要約へ縮小 | 27行を要約するために、方針（「同等の主要経路」等）を失う |
| 4 受入基準を移す | **移す対象が存在しない**（1.6.2節） |

#### 1.7.3 食い違いをどちらへ揃えるか

1.5.1節のとおり、**消えているのは常に `requirements.md` 側の語**であり、
いずれも範囲・方針・限定を示す。

⚠️ **`requirements.md` 側へ揃えるのが妥当である。**
USDM は要求文を振る舞いに絞ったが、**方針は要求文に残すべき情報である。**

ただし USDM は動詞を数えて分割する形式であるため、
**方針を書き戻すと動詞の数と分割の判断が変わる要求がある。**
6件それぞれについて、書き戻し方を個別に決める必要がある。

#### 1.7.4 残る作業

| 作業 | 規模 |
|---|---|
| 役割分担を両文書へ明記 | 各文書に数行 |
| 要求文の食い違い6件を解消 | 6件の書き戻しと、動詞の数・分割の再確認 |
| `REQ-C01` に受入基準との対応表を追加 | 1件 |
| サブ要求15件の差の洗い出し | **未着手**（1.5.3節） |

---

### 1.8 本節を追加した理由

**誤った材料の上で判断すると、判断も誤る。**

1.1節の「143行」は、第3章の行数をそのまま重複とみなしたものだった。
中身を確認せず、**章の見出しから内容を推測した**結果である。

2.4節の選択肢4も同様で、「受入基準は USDM にもあるはず」と考えて案を立てた。
**確認していなかった。**

⚠️ **判断材料を作るときこそ、実物を確認する。**

---

## 2. 検討した選択肢

### 2.1 選択肢1：`requirements.md` 第3章を USDM への参照に置き換える

要求文と受入基準は USDM が持ち、`requirements.md` は要求IDと実装単位の対応だけを残す。

⚠️ **この案は早い段階で落とした。**
143行を削るために、**58ファイルの参照の意味を変える**ことになる。
「`requirements.md` の `REQ-C05` を参照する」という記述が、実際には USDM を見なければならなくなる。
**コストが釣り合わない。**

### 2.2 選択肢2：両方を維持し、同期の規約を定める

現状のまま。143行が2箇所にあり続けるが、規約があれば片方だけ古くなるのを防げる。

### 2.3 選択肢3：役割を分ける

要求文の正本をどちらかに決め、他方は参照する。
たとえば「要求文と受入基準は USDM が正本。`requirements.md` 第3章は要求IDと一行要約のみ持つ」。

### 2.4 ~~選択肢4：受入基準だけを移す~~ → **成立しない**（1.6.2節）

**要求文はそのまま残し、受入基準だけを USDM へ移す。**

要求文は27件で27行、受入基準は約100行。
`requirements.md` は「何を作るか」を持ち、USDM は「どう振る舞うか」を持つ。

**重複は143行から27行へ減る。** かつ `requirements.md` だけで要求の一覧を把握できる。

⚠️ **本案は比較の途中で出たため、5軸の評価を行っていない。**

---

## 3. 選択肢2と3の比較（5軸）

### 軸1：同期の失敗がどう現れるか → **3が優る**

**選択肢2は「気づけない」失敗を生む。** 143行が2箇所にあり、片方だけ直したときに矛盾する。
**両方とも完全な文なので、読んだ人はどちらが古いか分からない。**

⚠️ **実例がある。** `roadmap.md` とルート `README.md` の進捗表を両方維持していたところ、
前者が「進行中（0-0 完了、0-1 が次）」、後者が「進行中」だけになっていた。
2026年9月1日に発見し、複製を解消した。

**選択肢3は「気づける」失敗になる。** 要約と全文は形が違うため、混同しにくい。

### 軸2：58ファイルの参照への影響 → **2が優る（ただし差は小さい）**

選択肢2は影響ゼロ。選択肢3は、要求文の全文を期待していた参照が一行要約しか得られなくなる。

⚠️ **参照の中身を見ると様相が違う。**
`requirements-analysis/` の20ファイルは「現行要件との比較」で参照しており**全文を必要とする**が、
`arc-*` の設計文書は `REQ-C05` というIDで対応を示しているだけで**全文は要らない。**

### 軸3：どちらが読まれるか → **2が明確に優る**

`requirements.md` は開発の入口であり、`AGENTS.md` の読み順に含まれる。

**選択肢3を採ると、要求を知るには2429行の USDM を読むことになる。負担が大きい。**
選択肢2なら `requirements.md` の143行で全要求を把握できる。

### 軸4：将来の変更 → **3がやや優る**

選択肢2は2箇所を直す（ただし143行なので量は小さい）。
選択肢3は USDM を直し、要約が古くなっていないか確かめる。
**要約は一行なので、要求文が変わっても要約が変わらないことが多い。**

### 軸5：実際に守れるか → **3が優る**

**選択肢2は規約を人が守る前提である。**
9月1日に `roadmap.md` の複製で失敗し、9月2日に「状態は正本が1つだけ持つ」と決めた。
**それでも9月5日、`AGENTS.md` へ 8.2 節を2度書いてしまった。**

選択肢3は構造で防ぐ。

### 集計

| 軸 | 優る案 |
|---|---|
| 1 同期の失敗の見え方 | 3 |
| 2 参照への影響 | 2 |
| **3 どちらが読まれるか** | **2** |
| 4 将来の変更 | 3 |
| 5 実際に守れるか | 3 |

**3対2で選択肢3だが、軸3の重みが大きく、決め手を欠く。**

⚠️ 1.6節の訂正により、選択肢2と3はいずれも過剰であることが判明した。選択肢5（1.7節）を参照。

---

## 4. 次に行うこと

~~0. 27件の要求文の差を洗い出す~~ → **メイン要求11件は完了（1.5節）。サブ要求15件は未検査。**
1. **選択肢4を5軸で評価する。** 比較の途中で出たため未評価である。
   特に軸3（どちらが読まれるか）で選択肢2と同等になる見込みがあり、
   その場合は**選択肢4が最有力になる。**
2. 5択を作る（[`../requirements-update-workflow.md`](../requirements-update-workflow.md)）。
   ⚠️ ただし選択肢が4つ挙がっており、5番目（LLMによる自動作成）を足すと定型に収まる。
3. 決定後、[`../requirements-usdm.md`](../requirements-usdm.md) 第13章の該当項目を解決済みにする。

⚠️ **急ぐ必要はない。** 現状は選択肢2（両方を維持）と同じ状態であり、
143行の重複が続くだけである。**誤った一本化のほうが害が大きい。**

---

## 5. 本書を作った理由

判断が疲れている状態で下すべきものではないと考えたため。

正本の一本化は58ファイルの参照に関わり、後戻りが難しい。
5軸の比較が3対2という結果で決め手を欠き、さらに比較の途中で選択肢4が出た。
**4つ目、5つ目の案が出る可能性もある。**

行数・参照の分布・5軸の比較は、この日に実測したものである。
**記録しなければ、次回また測り直すことになる。**
