# 「1.2.3」導入部分析の反映メモ

`section-1.2.3-external-help.md` を追加したことによる、既存文書への追記箇所。
**新規サブ要求ID 2件（`REQ-C07-S02`・`REQ-C09-S04`）を提案している点**が、
これまでの小節別分析（新規IDなし）と異なるため、反映時は注意すること。

---

## 0. 反映前の判断事項

本分析は**導入部だけを根拠とする暫定版**である。反映方法は次の2案から選ぶ。

### 案A：暫定版として先にコミットする（推奨）

小節本文の分析前でも、検証の記録として残す価値がある。
ただし、各文書に「暫定・小節分析後に再検証」と明記する。

### 案B：小節本文の分析完了まで保留する

`1.2.3.1` 以降を分析してから、まとめて1つの分析文書にする。
その場合、本ファイルは手元に置くだけでコミットしない。

以下は**案Aを選んだ場合**の追記内容である。

---

## 1. `docs/requirements-update-workflow.md`

### 1.1 冒頭のメタ情報（2行を置換）

**変更前**

```markdown
- 現在の進捗: 第3バッチの2件を選択済み。「1.2.2.5」の限定分析と`REQ-C04-S02`・`REQ-C09-S03`の詳細検証まで完了し、利用者の承認待ち
- 版: v0.19（「1.2.2.5」の限定分析と`REQ-C04-S02`・`REQ-C09-S03`の詳細検証を反映）
```

**変更後**

```markdown
- 現在の進捗: 第3バッチの2件を選択済み。「1.2.3」導入部の暫定分析まで完了し、小節本文の分析と利用者の承認待ち
- 版: v0.20（「1.2.3」導入部の暫定分析と新規サブ要求候補`REQ-C07-S02`・`REQ-C09-S04`を反映）
```

### 1.2 「3.1 進捗」の表（1行追加）

「1.2.2.5」の行の直後へ次を挿入する。

```markdown
| 節別増分 | 「1.2.3」導入部の理解ギャップ・補完源・前提知識の検証 | 暫定完了（`REQ-C07-S02`・`REQ-C09-S04`を候補化・小節本文は未分析） |
```

### 1.3 「7. 第3段階」（段落追加）

「1.2.2.5」の段落の直後へ次を挿入する。

```markdown
`1.2.3`の導入部の暫定分析は
[`requirements-analysis/section-1.2.3-external-help.md`](requirements-analysis/section-1.2.3-external-help.md)
に記録する。新しいメイン要求は追加せず、`REQ-C07:4`・`REQ-C09:4`の配下に新規サブ要求候補
`REQ-C07-S02`（補完源の管理）と`REQ-C09-S04`（前提知識ギャップと学習経路）を提案する。
`REQ-C05-S01`、`REQ-C07-S01`、`REQ-C10-S02`の根拠は補強するが、外部情報源の一次資料への優先、
未記載情報のLLMによる推定補完、非公開実装の推測再構成を要求しない。
本分析は導入部のみを対象とする暫定版であり、小節本文の取得後に再検証する。
```

### 1.4 「11. 完了条件」（項目追加）

「1.2.2」の8件完了の項目の直後へ次を挿入する。

```markdown
- 「1.2.3」の導入部について、既存要求で対応可能な障害と、新規サブ要求候補が区別されている。
- 「1.2.3」配下の小節本文を分析し、`REQ-C07-S02`・`REQ-C09-S04`の確定または統合が判断されている。
```

---

## 2. `docs/requirements-analysis/README.md`

一覧の末尾へ次の行を追加する（1.2.2.5 の行と同じ表形式に合わせる）。

```markdown
| [`section-1.2.3-external-help.md`](section-1.2.3-external-help.md) | 同書「1.2.3 自分以外の力も借りて論文を読み解くための技術」導入部 | 暫定分析（小節本文は未分析） | 新規メイン要求なし、`REQ-C07-S02`・`REQ-C09-S04`を候補化し、`REQ-C05-S01`・`REQ-C07-S01`・`REQ-C10-S02`の根拠を補強 |
```

---

## 3. `README.md`（リポジトリルート）

「1.2.2.5」の行の直後へ次を追加する。

```markdown
- 「1.2.3 自分以外の力も借りて論文を読み解くための技術」導入部の暫定分析: [`docs/requirements-analysis/section-1.2.3-external-help.md`](docs/requirements-analysis/section-1.2.3-external-help.md)
```

---

## 4. `docs/requirements-change-proposal.md`

**新規サブ要求IDを2件追加するため、これまでより影響が大きい。**
末尾へ次の節を追加する。

```markdown

## 「1.2.3」導入部の暫定分析

`REQ-C07-S02` および `REQ-C09-S04` は、
[`requirements-analysis/section-1.2.3-external-help.md`](requirements-analysis/section-1.2.3-external-help.md)
で提案した**新規サブ要求候補**である。導入部のみを根拠とする暫定案であり、
「1.2.3」配下の小節本文を分析した後に、確定または既存サブ要求への統合を判断する。

### 追加するサブ要求候補（暫定）

| 要求ID | 上位要求 | 内容 | 状態 |
| --- | --- | --- | --- |
| `REQ-C07-S02` | `REQ-C07:4` | 一次資料で不足する情報の補完源の管理 | 暫定・再検証待ち |
| `REQ-C09-S04` | `REQ-C09:4` | 前提知識ギャップの特定と学習経路の管理 | 暫定・再検証待ち |

要求文と受入基準は、同分析の第5節を暫定の正本とする。

### トレーサビリティ

「1.2.3」導入部 → SRC-123-01〜06 → `REQ-C07-S02` / `REQ-C09-S04`
→ 選択済み `REQ-C07:4` / `REQ-C09:4` のサブ要求 → 受入基準（同分析 5.1 / 5.2、暫定）

補強される既存要求は `REQ-C05-S01`（非公開実装の限界）、`REQ-C07-S01`（情報源の種類と時点）、
`REQ-C10-S02`（未報告情報の扱い）、`REQ-C08:4`（補完過程のプロヴェナンス）である。
```

---

## 5. `docs/requirements-analysis/section-1.2-reading-techniques.md`

「1.2」の上位分析にも、1.2.3 の小節別詳細として本分析へのリンクを追加することが望ましい。
既存の 1.2.1・1.2.2 へのリンク記載箇所と同じ形式に合わせる。

**注意**：このファイルは未取得のため、記載形式を確認してから追記すること。
